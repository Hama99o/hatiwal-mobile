# Hatiwal Mobile — Flow Register

The QA board for every Maestro flow in the app. **Regenerated** by
`./qa/qa.sh register` after each run.

> The `Status`, `Last run`, `Secs` and `API` columns are overwritten from real
> run data every time. The **`Triage`** and **`Notes`** columns are yours —
> they are parsed back out of this file and preserved. Put your verdict in
> `Triage` (`app-bug`, `flow-bug`, `fixed?`, `wontfix`) and the detail in `Notes`.

## Progress

**179 of 266 flows passing** · 84 still need attention

| Status | Count | Meaning |
|---|---:|---|
| PASS | 179 | green, and no backend error underneath |
| FAIL-assert | 82 | an assertion failed — real bug OR a stale selector, triage it |
| FAIL-redbox | 1 | a red box / JS console error appeared — real app error |
| (rig) | 2 | rig broke mid-run — result meaningless, re-run |
| UNTESTED | 1 | never executed |

### Definition of done

Every flow `PASS`, with zero `SILENT`. A `SILENT` row is not a pass: the
screen looked correct while the request failed, which is precisely the
bug class a user reports as "nothing happened".

## Flows

## `browse` — Buyer browse, search, filters, sort, listing detail, seller profile — a reserved listing stays searchable + messageable, and a held batch shows its hold

28/42 passing · 13 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `browse_all_categories` | PASS | run-561 | 174 |  | AxiosError |
| `browse_listings` | PASS | run-561 | 126 |  |  |
| `browse_sort_most_viewed` | PASS | run-561 | 134 | flow — chip strip. Labels are NOT stale (browse.json still has sort.mostViewed/nearest, FilterSheet SORT_OPTIONS renders 5 chips in a horizontal ScrollView). The blind `repeat 6x swipe 85%->20% @74%/68%` is not scrolling that strip at all: 6 iterations move ~2800dp, five chips need ~200. Replace with scrollUntilVisible direction:RIGHT. NOT yet verified on device. | AxiosError AxiosError |
| `browse_sort_nearest` | PASS | run-561 | 146 | flow — same chip strip as browse_sort_most_viewed. Extra wrinkle: `nearest` is NOT in SORT_OPTIONS; it is a separate chip (FilterSheet:409) that acquires location on tap, so it may also need a location fixture. |  |
| `categories_hub` | PASS | run-561 | 141 |  |  |
| `clear_all_filters` | PASS | run-561 | 135 |  |  |
| `filter_active_sellers` | PASS | run-561 | 134 |  |  |
| `filter_by_category` | PASS | run-561 | 128 |  |  |
| `filter_condition` | PASS | run-561 | 130 |  |  |
| `filter_price_range` | PASS | run-561 | 134 |  |  |
| `full_marketplace_cycle` | FAIL-assert | run-561 | 246 | flow (IME swallowed the card tap; the title assert could not catch it) — 0/4. Died at step-230 on `.*3,500`, and the fixture is fine: the API says the listing is priced 3500.0 and the flow's `.*` regex correctly covers the non-breaking space. The screenshot shows the app STILL ON THE SEARCH SCREEN — query typed, keyboard over the lower half, one card barely visible. The tap is by testID and correctly targeted; the keyboard was simply over the card. THE TITLE ASSERT ABOVE IT PASSED SPURIOUSLY: Maestro matches text ANYWHERE ON SCREEN, including inside an INPUT FIELD, and the query sits in the search box — so only the price exposed the failure. Audited all five search sites in this flow: THREE tap a card with no IME dismissal (the failing one plus two later), so fixing only the first would have moved the failure down the file. All three now dismiss (drag + `pressKey: Enter`, since the drag alone does not close the IME on the Bazaar grid) and PROVE navigation with `notVisible: browse-search-input`. | Four taps with the same search-box collision; three now erase and re-search first. |
| `listing_contact_whatsapp` | PASS | run-561 | 171 |  |  |
| `listing_detail` | PASS | run-561 | 179 | flow (racing the action bar) — bare `assertVisible: "Contact Seller"` on a row gated by the VIEWER resolving, while the listing's own data arrives first: there is a window where the page looks complete and has no buttons. The control is present and correct (listing.detail.contactSeller). Same cause as lifecycle_reserve, but this flow deliberately does NOT use open_thread_from_listing — it checks PRESENCE without tapping, because tapping opens the first-message sheet over the rest of the flow — so the wait went inline. |  |
| `listing_detail_held_units_transparency` | FAIL-assert | run-561 | 150 | REVERT CONFIRMED — no longer exits the app (run-494 fails on `listing-card` not visible, not the Android home screen). The hideKeyboard->drag revert worked here. Remaining failure is the scroll race. | [Failed] listing_detail_held_units_transparency (2m 22s) (Assertion is false: id: stock-badge-detail is visibl |
| `listing_detail_multi_quantity` | FAIL-assert | run-561 | 204 | RETRACTED — the clear_browse_filters fix (d99b486) is INERT here and its stated cause was WRONG. I read run-530's screenshot as two ACTIVE filter chips narrowing the grid; they are SAVED SEARCHES (BrowseHeader.tsx:419 renders <SavedSearches>; SavedSearchItem.tsx:105 draws the "1 new" badge from newMatchesCount) and saved searches do NOT filter the feed. The API was probed directly and filters correctly: category_id=1 -> 22 rows 0 outside Electronics; category_id=1&price_min=5000 -> 9 rows all >=7800; price_min=5000 -> 34 rows all >=5500. So there is no filtering bug and no narrowed feed. The helper call is KEPT as cheap insurance (filters really do persist between flows) but it taps browse-clear-filters, which only exists while the "N filters active" pill shows — absent in both screenshots. CAUSE STILL OPEN. Leading suspect, supported by measurement: the fixture sits at feed position 20 of 69 and per_page is 20, i.e. the LAST LOADED ROW of page 1, where centerElement:true has nothing below it to scroll past and burns its timeout. Verify that next; do NOT strip centerElement blindly (the run-263 note explains why it is needed for the "each" assert). | [Failed] listing_detail_multi_quantity (3m 16s) (No visible element found: "Wool Blanket Handmade King Size") |
| `listing_detail_offer` | PASS | run-561 | 159 | flow — converted to open_listing_by_title.yaml. |  |
| `listing_detail_offer_invalid` | PASS | run-561 | 163 | flow — converted to `_helpers/open_listing_by_title.yaml`. Failed with `No visible element found: "Wool Blanket Handmade King Size"` while the listing is FINE (API: 3366, active, in the browsable feed). Scrolling a virtualised grid for one title is the fragile part, and `centerElement: true` compounds it — an item landing in the last loaded row cannot be centred, the same unsatisfiable constraint that cost account_delete_cancel eight runs. This flow asserts NOTHING about the feed card, so the search helper fits exactly. |  |
| `listing_detail_price_drop_badge` | FAIL-assert | run-561 | 145 | RETRACTED — same as listing_detail_multi_quantity: the chips in run-530's screenshot are SAVED SEARCHES, not active filters, so the "narrowed to two cards" reading was wrong and the clear_browse_filters fix (d99b486) is inert. API verified correct (see that row). CAUSE STILL OPEN. The Lenovo sits at feed position 24 of 69 with per_page 20, i.e. on PAGE 2 — so the scroll must trigger pagination before the card can ever render, and centerElement:true compounds it. That, not a filter, is the thing to test. | [Failed] listing_detail_price_drop_badge (2m 16s) (No visible element found: "Lenovo ThinkPad Laptop Core i5 8 |
| `listing_detail_quantity_intent` | FAIL-assert | run-561 | 182 | TRIAGED run-530 (sha afaddcceb3e7, 0/3) — NOT the same cause as multi_quantity despite failing on the SAME title string. The end-of-flow screenshot shows the flow sitting on the listing detail of "Men Winter Jacket XL Black" (AFN 3,500, Clothes & Fashion, Kandahar) — it opened the WRONG LISTING, so the assertion on "Phone Case Silicone Clear - Wholesale" could never pass. Prime suspect: the RECENT SEARCHES overlay. run-530's price_drop_badge screenshot shows a stale recent-search chip reading "Men Winter Jacket XL..." sitting directly under the search box, exactly where a result card would be tapped. NEXT STEP: read the step-by-step screen-hierarchy to confirm the tap landed on the recent-search chip, then clear search history (browse.clearHistory) as well as filters. Do NOT apply the clear_browse_filters fix blind. | [Failed] listing_detail_quantity_intent (2m 54s) (Assertion is false: "Phone Case Silicone Clear - Wholesale"  |
| `listing_detail_report` | PASS | run-561 | 156 | flow — converted to open_listing_by_title.yaml. | RIG-004 tolerance; covers the detail-screen entry point. |
| `listing_detail_reserved_contactable` | PASS | run-561 | 135 |  |  |
| `listing_detail_save_unsave` | FAIL-assert | run-561 | 203 | flow (raced the tab bar re-mounting) — the open_listing_by_title conversion WORKED: the flow reaches the detail, asserts "Contact Seller" and taps save-toggle-button before failing. It then pops back with two conditional backs, which is exactly `_helpers/pop_to_tab_bar.yaml`'s pattern MINUS its final step — an extendedWaitUntil on a tab id. `when: notVisible` evaluates immediately and `waitForAnimationToEnd` returns when the UI settles, so neither waits for the TAB LAYOUT to re-mount after leaving a pushed screen; the tap raced it and reported "Element not found: saved-tab" on a bar that was on its way. The pops are correct and stay (listing detail lives outside app/(main)/(tabs)). Added the missing wait. | [Failed] listing_detail_save_unsave (3m 15s) (Assertion is false: id: listing-card is visible) |
| `listing_detail_saves_count` | FAIL-assert | run-561 | 150 | APP BUG (i18n) — THE FLOW IS RIGHT. 0/12 on '"Saved by.*" is visible'. run-561's hierarchy shows the listing page rendering the RAW TRANSLATION KEY, the literal text `listing.savesCount`, to the user. ListingDetail passed `count: formatNumber(...)` — a STRING — and i18next selects a plural form from `count`, which it cannot do for a formatted string. The key had ONLY savesCount_one/_other with no base key, so i18next fell back to rendering the key. FIXED 254b259. | 2026-09-15: fixed to the pattern already in the same file — listing.conversationsCount, TWO KEYS ABOVE savesCount, passes count (number) for plural selection AND display (formatted) for interpolation, and carries a base key beside its plural forms. savesCount had neither half. Now matches in all four locales, base key included as the belt-and-braces that stops a raw key reaching a user again. Swept the other ten call sites passing a formatted count: all fine, because their keys have a base form — this was the only plural-ONLY key, which is exactly why it was the only one that broke. Locale parity verified: 91 keys, identical across en/ps/fa/ur. |
| `listing_detail_share` | PASS | run-561 | 127 |  |  |
| `listing_detail_similar` | PASS | run-561 | 146 | TRIAGED — `"Similar Listings" is visible`. That section sits far down the detail screen; needs its own screenshot before a scroll is added. |  |
| `listing_detail_sold_recovery` | FAIL-assert | run-561 | 142 | TRIAGED — `No visible element found: id: seller-profile-link` (a scrollUntilVisible timeout). Needs its own screenshot; check for centerElement on a last element. | Optional tap paired with an optional assert checked nothing; now a when: conditional. |
| `listing_detail_sold_state` | PASS | run-561 | 121 | TRIAGED — `"Seller" is visible`. Needs its own screenshot. |  |
| `listing_detail_views_count` | PASS | run-561 | 176 |  |  |
| `not_interested` | PASS | run-561 | 127 |  |  |
| `saved_search_apply` | FAIL-assert | run-561 | 147 | TRIAGED — 0/3 on `"Saved search" is visible`. Needs its own screenshot; not yet investigated. | [Failed] saved_search_apply (2m 20s) (Assertion is false: "Saved search" is visible) |
| `scroll_to_top` | PASS | run-561 | 127 |  |  |
| `search_empty_state` | FAIL-assert | run-561 | 157 | flow (IME over the results area + inherited filters) — `"No listings found" is visible` fails because that copy renders in the RESULTS AREA, exactly where the keyboard sits after typing. run-530's screenshot: query in the box, filter chips, a small gap, keypad over everything below. THE CONTROL: search_listings allows 30s on this same string and still times out, so the element never becomes VISIBLE, only present — waiting cannot fix it. Added drag + `pressKey: Enter` (never Back, which this flow's sibling notes CANCELS the search). Also added clear_browse_filters: the screenshot shows it searching under "1 filter active" (Electronics, Within 5 km) inherited from an earlier flow, and the two browse flows that already open that way are search_listings and full_marketplace_cycle. | [Failed] search_empty_state (2m 29s) (Assertion is false: "Bazaar" is visible) |
| `search_listings` | FAIL-assert | run-561 | 186 | flow (IME over the results area) — same cause as search_empty_state and the CONTROL that proves it is not a timing problem: this flow already allows `extendedWaitUntil ... timeout: 30000` on "No listings found" and still times out, so the element is present but never ON SCREEN. It already clears filters, so filters are not the cause either. Added drag + `pressKey: Enter` after the query. | [Failed] search_listings (2m 58s) (Element not found: Text matching regex: Reset filters) |
| `search_with_filter` | PASS | run-561 | 144 |  |  |
| `seller_profile` | PASS | run-561 | 159 | flow — converted to `_helpers/open_listing_by_title.yaml` after the shape check I queued last tick came back clean: scroll -> tap, NO feed-card assertion, so it only needs the listing OPEN. Failed with `No visible element found: "Wool Blanket Handmade King Size"` while the listing is active and in the feed (API 3366). Scrolling a virtualised grid for one title is the fragile part, and centerElement compounds it. |  |
| `seller_profile_from_listing` | PASS | run-561 | 135 |  |  |
| `seller_response_rate_badge` | FAIL-assert ⟳stale | run-561 | 171 | FLOW-BUG-FIXED 3f62ce7 — REGEX, NOT THE APP. 0/5 at sha 633cd56841b2 on '"[0-9]+% reply rate" is visible'. Maestro matches `text` against the WHOLE node. ResponseRateBadge.tsx:41 builds ONE Text node `${ratePart} · ${timePart}` = "94% reply rate · Usually responds within an hour", so the bare pattern matched a prefix only and was unsatisfiable. Proof was already in the flow: the assert two steps up reads ".*Usually responds within.*" and PASSES on that same node in the same run. Wrapped both in .*. NEEDS A DEVICE RUN at sha f67ff204863b. | 2026-09-15: NOT a data gap — the API returns response_rate_percent=94 and response_time_label="within_one_hour" for listing 5062, checked before touching the flow. Rule written into QA_HANDBOOK.md. Swept all flows for the shape: 7 regex-bearing selectors, this the only broken one; the other 6 describe standalone labels where a full match is correct and were left alone. |
| `subcategory_drilldown` | PASS | run-561 | 139 | flow — chip reads "Subcategory: Phones & Tablets"; the two chip asserts still said "Phones" | Seed is "Phones & Tablets"; 5 refs widened. One was assertNotVisible "Phones" — a FALSE PASS. |
| `user_profile_empty_listings` | FAIL-assert | run-561 | 138 | TRIAGED — NOT convertible, and the reason matters: `No visible element found: ".*Ahmad Karimi.*"` looks like the same feed-scroll failure, but it is not a feed scroll at all. It walks the CONVERSATIONS list for a PERSON'S NAME and then asserts "Type a message...", a chat composer. Sending it through a listing-search helper would be nonsense. Needs its own look at how that list is reached. | Premise impossible: asserted a listing's own seller has 0 listings. Reaches a 0-listing profile via chat. |
| `user_profile_listing_grid` | PASS | run-561 | 134 | flow | Grid sits below the profile header; assertVisible does not scroll. Added both ways. |
| `user_profile_stats` | PASS | run-561 | 127 | flow — asserted a "Message" button the profile has never had (contact is per-listing by design) | Hardcoded "2024"; member_since renders "August 2026" as one node. Year-shaped pattern. |
| `view_mode_toggle` | FAIL-assert | run-561 | 163 | REVERT CONFIRMED — PASSED in run-494 after the hideKeyboard->drag revert. | HOLLOW: every tap optional, only assertion was the always-present tab label. Rewritten. |

## `chat` — Conversations, messages, offers, meetup arrangement, read state — mark-sold one-tap from the thread, place/release a hold with the buyer you're already talking to

33/49 passing · 12 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `archive_conversation` | PASS | run-575 | 245 | PASS on a quiet box (run-523) — confirms the extendedWaitUntil-before-conversation-row fix. |  |
| `block_from_conversation` | PASS | run-575 | 172 | CANDIDATE APP BUG 8 KILLED 2026-09-13 — the app is CORRECT; this is CAUSE C, self-interference. FLAKY 6/15 at sha 15a01b909c25, so not patched. PROOF: (1) the API is clean — blocking then unblocking over HTTP returns blocked_by_me/blocked_with_participant BOTH false at 0ms, no lag, so there is no stale read to blame. (2) The flow is not asserting too fast — it already does extendedWaitUntil "Type a message..." timeout 15000 before the assert. (3) Conversation.tsx:565 sets isBlocked = conv.blockedWithParticipant, which is the OR OF BOTH DIRECTIONS (see the comment at :201), and unblockMutation.onSuccess (:1397) deliberately clears only blockedByMe, NOT isBlocked, because removing MY block does not lift THEIRS. canSend (:1478) gates the composer on isBlocked. So when the OTHER party still blocks the viewer the composer correctly never returns. WHO LEAVES RESIDUE: report_user_then_block, block_user, block_prevents_message and block_user_hides_listings all block and run in the same cycle; block_user_hides_listings has been seen hitting the 600s cap, so its cleanup does not always run. THE FIX IS FIXTURE ISOLATION, not app code: clear BOTH directions at flow start before asserting the composer returns. Do NOT file an app bug. useFocusEffect (:712) does reload, so the screen recovers on navigate-away-and-back — the question the campaign had open. | 2026-09-05 CAUSE FOUND, board BLK-2. The block SUCCEEDS server-side (INSERT+COMMIT in the API log; endpoint returns 204 by hand) while the app shows "Could not block user. Try again." 401s in the same window and devise rotates the token per request; http.ts clears the session on any 401. Load-sensitive: passed at 147s on a quiet host. Supersedes the older #312 note. |
| `chat_older_messages_pagination` | PASS | run-575 | 158 |  | AxiosError |
| `composer_draft` | PASS | run-575 | 201 | flow | Tapped a title that was sitting in the search box, so the tap hit the input. |
| `conversation_archive` | PASS | run-575 | 176 | PASS on a quiet box (run-523) — confirms the extendedWaitUntil-before-conversation-row fix; this is the flow whose hierarchy dump originally proved the list had not loaded. |  |
| `conversation_delete` | PASS | run-575 | 165 | flow — NOT a My Shop case, checked before converting: it taps chat-tab and scrolls the CONVERSATIONS list, so search_my_shop.yaml would be the wrong tool. Its fixture "QA Disposable conversation_delete" IS present and active. Needs the conversations search bar instead — same idea, different list. | 2026-09-02: soft-DELETED its own fixture. Targeted the shared Xiaomi thread as "safe because SOLD"; the delete stamped buyer_deleted_at (09-01 17:54) so not_deleted_for hid it from the buyer for good and every later run failed. App was correct. Now owns "QA Disposable conversation_delete"; the seed clears delete/archive flags on disposable convos each run. |
| `conversation_read_status` | PASS | run-575 | 190 | flow — same list race as conversation_archive. 1 `Network Error` line in its logcat. | mark_unread needs an INBOUND message; index 0 was QA debris with none. Pinned via helper. |
| `conversations-search` | PASS | run-575 | 205 |  | AxiosError |
| `conversations_empty_state` | FAIL-assert ⟳stale | run-575 | 164 | STALE VERDICT — NOT A CURRENT FAILURE. run-575 shows it failing on "Element not found: Id matching regex: register-email-input", but that run executed sha 80bf7efcfd5e while the file on disk is 223f8939320d. The register-scroll fix (b3f5a93, 09-15 12:40) is on disk and the run predates it, so this verdict describes a file that no longer exists. The hierarchy matches the OLD file: register form filled to the phone field, keyboard_holder mounted, email input below it behind the IME. NEEDS A DEVICE RUN at sha 223f8939320d; do not re-triage before then. | 2026-09-15: worth noting for the run-575 tally — its "14 fails" includes at least this one stale row, so the live failure count is lower than the file says. This is exactly what the run-sha vs disk-sha rule is for, and the only reason it was caught is that the sha was checked before the evidence was believed. |
| `conversations_filter` | PASS | run-575 | 233 | rig/env — inside the 14:47-14:59 window where four flows failed consecutively; asserts `profile-tab` (a signed-in tab bar) and does not get one. 1 `Network Error` line. | 2026-09-02: asserted the "All caught up!" EMPTY state on the Unread tab, which 3 sibling flows mutate and the seed gives exactly ONE unread. Order-dependent. Now branches with runFlow: when (native in 2.7.0). |
| `conversations_list` | PASS | run-575 | 170 | rig/env — same window. 2 `Network Error` lines. The 'Unread' label is NOT stale (chat.json filters.unread = 'Unread', and it is visible in conversation_archive's screenshot). |  |
| `conversations_role_filter` | FAIL-assert ⟳stale | run-575 | 258 | FLOW-BUG-FIXED 3db2973 — SCROLLED PAST IT. run-575 0/27 died at step 096, the FIRST bike scroll, before any chip is tapped; every later step never ran. The debug dir kept the screen hierarchy for that step and it shows rows 18-19 on screen (role-pill-2928 / role-pill-2929) — the END of a 19-row list. Live inbox measured 2026-09-15: Mountain Bike is row 6, first Lenovo row 7, adjacent, bike ABOVE. Scrolling DOWN to Lenovo parks the bike just off the TOP edge; the next DOWN scroll then spent its 20s dragging to the bottom. Fixed by scrolling in list order (bike, then Lenovo) in both mixed-inbox pairs; the two scrolls under an active chip are a different list and were left alone. THIRD instance of this shape after 07bf502 (edit_listing_all_fields) and b3f5a93 (five register flows) — a successful scrollUntilVisible leaves the list where it stopped and the next command inherits that offset. NEEDS A DEVICE RUN at sha 16c1abf4ce3f to confirm. | 2026-09-15: header counts re-measured and corrected — it claimed rows 10/11 of a 24-thread inbox with badge threads at 18-22; actually rows 6/7 of 19 with five extra Lenovos at 14-18. All three numbers were stale and the real adjacency is what makes the scroll order matter. NB the assertNotVisible asserts are weak by nature — filtered-out and below-the-fold are indistinguishable to Maestro; documented in the flow. ALSO: this flow is the BLEED SOURCE for dead_end_notice_sold — it calls login_seller twice and login.yaml's sign-out scroll is optional:true, so the seller identity survives into later flows. Fixing the scroll does NOT fix that; session isolation is still open. |
| `dead_end_notice_absent_when_active` | PASS | run-575 | 252 | FALSE-PASS | 2026-09-15. PASSES, and that is the problem — it asserts the unavailable notice is ABSENT, which is ALSO what a seller sees (the notice is buyer-facing, gated behind !isOwner). So it passes whether or not the session identity is correct, and it ran at 12:38 between the seller-login flow that caused the bleed and dead_end_notice_sold, which failed on it at 12:42. A green row here is not evidence the identity was right. Needs a positive identity assertion (assert the viewer is the buyer) before its pass means anything. |
| `dead_end_notice_sold` | FAIL-assert | run-575 | 251 | SESSION-BLEED | 2026-09-15, run-575. NOT an app bug — the app is correct and the flow is the wrong USER. The screenshot shows the sold Xiaomi thread with NO `listing-unavailable-notice` and the SELLER quick replies ("Yes, it's available", "Let's meet at [place]" = chat.quickReplies.seller.*). `QuickReplies role={isOwner ? 'seller' : 'buyer'}`, so isOwner was TRUE — the app was logged in as seller 594 while this flow needs buyer 593. Verified against the DB: listing 5071 owner=594 status=sold, conversation buyer=593 seller=594, and the flow calls _helpers/login.yaml which defaults to buyer@hatiwal.test. The bleed source is the flow THREE before it in the same pass — conversations_role_filter uses `login_seller` twice — and login.yaml's identity switch is `optional: true` on the scroll to `sign-out-button`, so when that button is not reached the helper SILENTLY keeps the previous session. Same root cause as listing_actions_sheet and the nine 'Make an Offer' flows. |
| `delete_message` | PASS | run-575 | 256 | app+flow | 2026-09-02: failed on "Delete message" with the message sent and visible. Cause was the APP — the bubble sat behind the composer bar so the long press hit the bar and no sheet opened. Fixed structurally in 61ad571 (list ends at the bar). Flow also now waits for the sheet's animation. |
| `jump_to_latest` | PASS | run-575 | 320 | flow — same login skip. Fails on `No visible element found: "Switch to .*"`, which is login.yaml's post-login mode check; it can only be reached authenticated. | 2026-09-05 "Switch to .*" was OFF-SCREEN on a scrolled profile, not missing — the app was signed in and healthy in the screenshot. ensure_buyer_mode now scrolls UP to recover it (UP matters: a previous fix used a DOWN scroll and carried the toggle further away). |
| `lifecycle_from_chat` | PASS | run-575 | 406 | REGRESSION vs run-523 — passed there, fails here on `"View Listing" is visible`. Not obviously related to any change made in this campaign; needs its own read from run-526's artifacts before anything is done to it. |  |
| `mark_read` | PASS | run-575 | 243 | flow — asserts the LOGIN tagline 'Buy and sell locally in Afghanistan' is visible and it is not. Needs its own read once the login helper is fixed; the session state it assumes is the thing currently unstable. | Same unrepliable-thread trap. |
| `mark_read_end_to_end` | PASS | run-575 | 217 | FIXED run-533 (sha 0b095ba0008a, 3/16 -> new sha 827480efaf2c). CAUSE A, rendered but not ON SCREEN. The bare `assertVisible: "Today"` at :97 fires while the thread is pinned to the BOTTOM after sending a message, but the day separator sits above the FIRST message of today — off the top of the viewport on any thread with more than a screenful of today's messages. run-533's end-of-flow SCREENSHOT settles it: thread open and rendering (2:15 PM, 2:28 PM, 3:03 PM, 3:20 PM), the "Unread messages" divider plainly visible, and NO "Today" separator on screen. The separator was never missing. That also explains the 3/16 — it passed only when the thread happened to be short enough for "Today" to fit, which depends on how many messages earlier flows left. Fix: scrollUntilVisible direction UP before the assert, the same idiom this file already uses a few lines below for the Unread divider. NO centerElement — the separator can be the topmost element, and centring something with nothing above it is unsatisfiable. The selector is current: chat.json:5 "today": "Today". AWAITING VERDICT. | 2026-09-02: asserted an unread badge exists then tapped conversation-row index 0 — the newest thread, not necessarily the unread one. The divider only exists inside a thread with unread messages. Now taps unread-badge, which bubbles to its own row. |
| `meetup_decline` | PASS | run-575 | 401 | flow — fails on login_seller.yaml's mode gate (`"Switch to .*" is visible`, line 342), NOT a missing toggle: the hierarchy at the failing step is the Profile screen scrolled DOWN into settings (ACTIVITY, Appearance, Blocked Users, Language), and the toggle is at the TOP. The gate does not scroll — the file says so itself at line 118. Fix written up as PENDING fix 9 for that file's owner; the working pattern is the UP scroll in _helpers/ensure_buyer_mode.yaml:114. | reload-corrupted in run-232, AND a real defect underneath: it tapped Decline on a proposal nothing seeds (grep meetup in e2e.rb = 0), and Decline needs `!isMine`. Now two-party via _helpers/propose_meetup. 1bdaa76 |
| `meetup_full_cycle` | FAIL-assert | run-575 | 283 | NEEDS ONE DEVICE PROBE — do not patch from disk. Failed '"Contact Seller" is visible' at command 105, inside open_thread_from_listing. The action bar is rendered OUTSIDE the ScrollView ("Sticky action bar", ListingDetail.tsx:1050) so it is always on screen — its absence means canContact was FALSE, i.e. isOwnListing or status not in (active,reserved). BUT the anomaly is upstream: command 101 `assertVisible ${TITLE}` COMPLETED, so open_listing_by_title proved it had opened the requested listing — and four commands later the hierarchy is a DIFFERENT listing, "Men Winter Jacket XL Black" (AFN 3,500, Clothes & Fashion), which is open_listing_by_title.yaml's own DEFAULT title. The flow passes TITLE: "Phone Case Silicone Clear - Wholesale" (AFN 400). | 2026-09-15: ruled OUT — (a) the env-override mechanism, which works: guest_offer_redirect, block_user, seller_profile, listing_detail, listing_detail_offer, listing_detail_report and listing_detail_offer_invalid all pass TITLE the same way and PASS; (b) the call syntax, which is the documented runFlow{file,env} form; (c) API search being broken — `search` (not `q`) is the real param and it filters correctly, 2/2 matching. My earlier `q=` probe used the wrong parameter and proved nothing. Also note the jacket is no longer in the public index (69 listings, absent), so it is sold/unbrowsable NOW — which would independently make canContact false, but says nothing about its state during run-575. NEXT PROBE: a device run watching what sits on screen between commands 101 and 105. |
| `meetup_proposal` | PASS | run-575 | 250 | flow | CONFIRMED reload artefact — its logcat carries `Destroying ReactContext`: I saved a src/ file mid-run and the dev client reloaded. No app or flow defect known. Submit is now by ID anyway (the label swaps to "Sending…"). 1bdaa76 919aeb2 |
| `meetup_proposed_bubble_ui` | PASS | run-575 | 317 | flow | PROVEN defect, no reload in its logcat: filled only the place, and the app rightly refuses without a time (handlePropose sets timeError). Now fills both. 1bdaa76 |
| `meetup_respond` | FAIL-assert | run-575 | 435 | SESSION-BLEED (not fixed — root cause is login.yaml, see Notes). Failed "Element not found: Text matching regex: .*Accept.*". NOT a missing button and NOT the scroll shape. MessageBubble.tsx renders Accept/Decline under `!isMine && onMeetupRespond` — "only for the recipient". run-575's hierarchy shows BOTH meetup cards with right-aligned timestamps (isMine) while the app is in SELLER context (Mark Sold + chat.quickReplies.seller.* "Yes, it's available" / "The price is firm"). propose_meetup.yaml sends "Kabul City Center" / "Saturday 3pm" — the second card — so the proposal was sent AS THE SELLER during the flow's BUYER leg. login.yaml at step 0 never switched away from a previous flow's seller session. | 2026-09-15: login.yaml documents this exact symptom for meetup_decline — "ran its whole second leg still signed in as the buyer, so Accept/Decline, gated on !isMine, never rendered and the flow blamed a missing button." The hole is that the sign-out is reached by a `scrollUntilVisible ... optional: true`; when the button is not reached, nothing happens and nothing is reported. NOT PATCHED TODAY: the fix belongs in login.yaml, which ~190 flows include, the natural guard needs a multi-key `when:` (visible + notVisible) and there is ZERO precedent for that construct in all 312 flows — introducing it unverified, with no device, could turn the whole suite red. Proposed to the peer instead. |
| `meetup_validation` | PASS | run-575 | 251 | flow | CONFIRMED reload artefact (`Destroying ReactContext` in logcat). UI-043 withdrawn. Inline-error coverage (place/time required) kept intact. 1bdaa76 919aeb2 |
| `message_long_text` | PASS | run-575 | 277 | flow | Asserted 27 chars of the 366-char message it sent. Now spans both ends. |
| `offer_counter_flow` | FAIL-assert | run-575 | 251 | SESSION-BLEED (2nd CONFIRMED INSTANCE — not fixed, root cause is login.yaml). Failed '"Make an Offer" is visible'. run-575's SCREENSHOT is decisive where the hierarchy was not: the sticky action bar renders a lone BAN PILL. ListingDetail.tsx:1199 shows that is exclusively the `isOwnListing` branch (<Ban size={16}/> + ownListingNotice). Excluded by elimination too: listing 5060 is status=ACTIVE so isListingDeadEnd is false, and canContactListing returns false ONLY for an owner on an active listing. The listing is owned by 594 (seller@hatiwal.test) and the flow logs in via login.yaml as the BUYER — so the app was running as 594. | 2026-09-15: the app is CORRECT — it hides Make an Offer from an owner. Four offer flows share this exact 12-node screen (jacket 5060: AFN 3,500, Clothes & Fashion, 143 views) and so does meetup_full_cycle. ALL the offer fixtures — 5060 jacket, 5061 wool socks, 5062 phone case, 5068 wool blanket — are active, negotiable and owned by 594, so the identity fault applies whichever one is open. login.yaml already documents this shape: "reserve_after_accept's screenshot is that branch: a lone Ban pill where the action row belongs." SECOND confirmed instance after meetup_respond — login.yaml's guard is now the top device item. |
| `offer_in_existing_thread` | FAIL-assert ⟳stale | run-575 | 236 | FLOW-BUG-FIXED 4a427e87ef61 — SEVENTH OCCLUSION INSTANCE, keyboard variety. Failed '"Send Offer" is visible' asserted immediately after opening the sheet. OfferSheet AUTOFOCUSES the amount input, so the IME is already up, and Send Offer (OfferSheet.tsx:459) is the submit at the BOTTOM of the sheet, behind it. run-575 step 93's hierarchy has the keyboard mounted (y=1184-1280) with the sheet's own rows — "Make an Offer", "Listed price: AFN 400 (each)", "Your offer", "Quick amounts" — all present. The sheet was open and correct. Asserting offer-amount-input instead. NEEDS A DEVICE RUN at sha 4a427e87ef61. | 2026-09-15: listing_detail_offer PASSES and never asserts the button early — it taps the field, types, then taps Send Offer. Matched that shape rather than adding a keyboard dismissal: hideKeyboard is Android BACK and inside a MODAL sheet it would close the sheet, and dismiss_keyboard_by_drag only works where the list opts into keyboardDismissMode=on-drag. |
| `offer_quantity_round_trip` | FAIL-assert | run-575 | 293 | SESSION-BLEED (2nd CONFIRMED INSTANCE — not fixed, root cause is login.yaml). Failed '"Make an Offer" is visible'. run-575's SCREENSHOT is decisive where the hierarchy was not: the sticky action bar renders a lone BAN PILL. ListingDetail.tsx:1199 shows that is exclusively the `isOwnListing` branch (<Ban size={16}/> + ownListingNotice). Excluded by elimination too: listing 5060 is status=ACTIVE so isListingDeadEnd is false, and canContactListing returns false ONLY for an owner on an active listing. The listing is owned by 594 (seller@hatiwal.test) and the flow logs in via login.yaml as the BUYER — so the app was running as 594. NOTE: this flow requests TITLE="Wool Socks Bulk Pack - 12 Pairs" yet its screen is the JACKET — the same wrong-listing anomaly as meetup_full_cycle, unresolved and needing the same probe. It does not change the diagnosis: every candidate fixture is owned by 594. | 2026-09-15: the app is CORRECT — it hides Make an Offer from an owner. Four offer flows share this exact 12-node screen (jacket 5060: AFN 3,500, Clothes & Fashion, 143 views) and so does meetup_full_cycle. ALL the offer fixtures — 5060 jacket, 5061 wool socks, 5062 phone case, 5068 wool blanket — are active, negotiable and owned by 594, so the identity fault applies whichever one is open. login.yaml already documents this shape: "reserve_after_accept's screenshot is that branch: a lone Ban pill where the action row belongs." SECOND confirmed instance after meetup_respond — login.yaml's guard is now the top device item. |
| `offer_send_and_accept` | FAIL-assert | run-575 | 329 | SESSION-BLEED (2nd CONFIRMED INSTANCE — not fixed, root cause is login.yaml). Failed '"Make an Offer" is visible'. run-575's SCREENSHOT is decisive where the hierarchy was not: the sticky action bar renders a lone BAN PILL. ListingDetail.tsx:1199 shows that is exclusively the `isOwnListing` branch (<Ban size={16}/> + ownListingNotice). Excluded by elimination too: listing 5060 is status=ACTIVE so isListingDeadEnd is false, and canContactListing returns false ONLY for an owner on an active listing. The listing is owned by 594 (seller@hatiwal.test) and the flow logs in via login.yaml as the BUYER — so the app was running as 594. | 2026-09-15: the app is CORRECT — it hides Make an Offer from an owner. Four offer flows share this exact 12-node screen (jacket 5060: AFN 3,500, Clothes & Fashion, 143 views) and so does meetup_full_cycle. ALL the offer fixtures — 5060 jacket, 5061 wool socks, 5062 phone case, 5068 wool blanket — are active, negotiable and owned by 594, so the identity fault applies whichever one is open. login.yaml already documents this shape: "reserve_after_accept's screenshot is that branch: a lone Ban pill where the action row belongs." SECOND confirmed instance after meetup_respond — login.yaml's guard is now the top device item. |
| `offer_send_and_decline` | FAIL-assert | run-575 | 271 | SESSION-BLEED (2nd CONFIRMED INSTANCE — not fixed, root cause is login.yaml). Failed '"Make an Offer" is visible'. run-575's SCREENSHOT is decisive where the hierarchy was not: the sticky action bar renders a lone BAN PILL. ListingDetail.tsx:1199 shows that is exclusively the `isOwnListing` branch (<Ban size={16}/> + ownListingNotice). Excluded by elimination too: listing 5060 is status=ACTIVE so isListingDeadEnd is false, and canContactListing returns false ONLY for an owner on an active listing. The listing is owned by 594 (seller@hatiwal.test) and the flow logs in via login.yaml as the BUYER — so the app was running as 594. | 2026-09-15: the app is CORRECT — it hides Make an Offer from an owner. Four offer flows share this exact 12-node screen (jacket 5060: AFN 3,500, Clothes & Fashion, 143 views) and so does meetup_full_cycle. ALL the offer fixtures — 5060 jacket, 5061 wool socks, 5062 phone case, 5068 wool blanket — are active, negotiable and owned by 594, so the identity fault applies whichever one is open. login.yaml already documents this shape: "reserve_after_accept's screenshot is that branch: a lone Ban pill where the action row belongs." SECOND confirmed instance after meetup_respond — login.yaml's guard is now the top device item. |
| `place_and_release_hold` | PASS | run-575 | 214 | PASS — first flow to run with fixes 6+4 in place. |  |
| `quick_replies` | FAIL-assert ⟳stale | run-575 | 275 | FLOW-BUG-FIXED 5d6e88a4aa34 — THE TAP MOVED THE CARET. Failed asserting "Is this still available? Please let me know." because what it actually sent was "Is this still availa Please let me know.ble?" — the phrase split after "availa" with the new text spliced in. The flow tapped the quick-reply chip (which fills the composer), then tapped the SAME TEXT again to focus the field; Maestro taps an element's CENTRE, so the caret landed mid-string and inputText inserted there. Removed the second tap. NEEDS A DEVICE RUN at sha 5d6e88a4aa34. | 2026-09-15: NOT an app bug — a tap in the middle of a text field is supposed to move the caret there, and the re-tap was never needed: handleQuickReplySelect (Conversation.tsx:1434) focuses the input itself, its comment saying "cursor lands at the end". The assert between the two taps could not disambiguate them, because after the first tap that string is on screen TWICE — as the chip and as the composer's contents. Swept the suite: 38 sites tap text then inputText, 37 tap a PLACEHOLDER (empty field, caret has nowhere else to go) including FilterSheet's placeholder="0", which looks like a value. This was the only real one. |
| `report_participant` | FAIL-assert | run-575 | 325 | APP BUG — THE FLOW IS RIGHT. Failed waiting for '.*already reported.*'. The report SUCCEEDED and the duplicate WAS rejected: a previous author recorded run-526's Rails log showing POST /api/v1/reports -> 201 then -> 422. ReportSheet.tsx:206 maps that 422 to toast.error(report.errors.duplicate) = "You have already reported this.", which the flow's regex matches. The user never sees it: <Toaster> is mounted at app/_layout.tsx:115, at the ROOT of the tree, and on Android a RN <Modal> is a SEPARATE NATIVE WINDOW — so the toast renders BEHIND the still-open sheet. Every error path in ReportSheet leaves the sheet open, which is exactly when the toast is invisible; the success path closes it first, which is why that one works. | 2026-09-15: FIXED IN THE APP, not the flow — the error is now ALSO rendered inline inside the sheet (testID report-submit-error), alongside the existing reasonRequired error, with the toast kept for the paths where the sheet has closed. This is the owner's silent-failure class exactly: tap Submit, backend returns 422, nothing on screen changes. Scope beyond this sheet: six components pair a <Modal> with toast calls (Browse, Conversation, ReviewPromptSheet, ListingForm, ReportSheet, ListingDetail) — only ReportSheet's duplicate path is PROVEN, the others are candidates needing a per-path check. NB the handbook already recorded a sibling of this: the report success toast covered by the "also block this user?" alert. |
| `reserve_after_accept` | FAIL-assert | run-575 | 260 | SESSION-BLEED (not fixed — root cause is login.yaml). Failed '"Make an Offer" is visible', the owner-gated affordance. Same shape as the offer cluster and reserved_buyer/lifecycle_reserve: canContactListing returns false only for an owner on an active listing, and ListingDetail.tsx:1199 renders the lone Ban pill for that branch. login.yaml NAMES this flow in its own comment as a victim of its missing guard. | 2026-09-15: family is now TEN — 4 offer flows, meetup_respond, meetup_full_cycle, reserved_buyer, lifecycle_reserve, and these two. One helper, no flow-level patch appropriate. The guard needs a device. |
| `reserve_after_buyer_accepts_counter` | FAIL-assert | run-575 | 270 | SESSION-BLEED (not fixed — root cause is login.yaml). Failed '"Make an Offer" is visible', the owner-gated affordance. Same shape as the offer cluster and reserved_buyer/lifecycle_reserve: canContactListing returns false only for an owner on an active listing, and ListingDetail.tsx:1199 renders the lone Ban pill for that branch. login.yaml NAMES this flow in its own comment as a victim of its missing guard. | 2026-09-15: family is now TEN — 4 offer flows, meetup_respond, meetup_full_cycle, reserved_buyer, lifecycle_reserve, and these two. One helper, no flow-level patch appropriate. The guard needs a device. |
| `reserved_sold_dead_end_notice` | PASS | run-575 | 428 | rig — TIMEOUT, not hung, and now fixed at the rig. Hit the 600s cap in BOTH run-521 and run-523 while neighbours passed in 282-354s, so it is not host load. It runs BOTH login helpers (login_seller.yaml then login.yaml) and its debug screenshots reach step-125, the SECOND login's notification prompt — two cold stopApp/launchApp/bundle/onboarding/sign-in cycles plus its own work does not fit in ten minutes. qa/lib/flows.sh now gives any flow that references both helpers 2x FLOW_TIMEOUT. | Five logins could not fit FLOW_TIMEOUT=600; split into three flows, only this one mutates. |
| `scroll_to_latest` | PASS | run-575 | 579 | rig — TIMEOUT, not hung, same fix. Sends SEVEN messages (repeat times:6 plus one) and then cold-restarts the app with a 120s wait for chat-tab; summed declared timeouts alone are 320s. Also timed out in run-521. Covered by the 2x budget for double-login flows. | 2026-09-03 SOLVED: the meetup sheet was drawn UNDER the Android keyboard, so Time and Propose were unreachable when the sheet opened with the IME already up — the ordinary path, which no meetup flow covered. Fixed d46c896; PASS at BOTH widths after the rebuild. Its earlier 600s timeout at 360dp was a SYMPTOM of the same bug (dead waits), not a ceiling that needed raising. |
| `send_message` | PASS | run-575 | 286 | rig/env — recorded with kind=rig_fail in run-521, a pass that ran under HOST CONTENTION: session 1's emulator plus ANOTHER agent's three chrome-headless processes, ffmpeg and Rails on the same box. The pass averaged 310s/flow (15205s for 49) against a ~208s baseline and the driver began backing off at load 16.9. NOT an app or flow defect — re-run on a quiet box before triaging. (Note results.jsonl marks these via `kind`, while `result` still reads "fail" — filtering on result alone misses them.) | AxiosError |
| `send_message_double_tap` | PASS | run-575 | 260 | rig/env — recorded with kind=rig_fail in run-521, a pass that ran under HOST CONTENTION: session 1's emulator plus ANOTHER agent's three chrome-headless processes, ffmpeg and Rails on the same box. The pass averaged 310s/flow (15205s for 49) against a ~208s baseline and the driver began backing off at load 16.9. NOT an app or flow defect — re-run on a quiet box before triaging. (Note results.jsonl marks these via `kind`, while `result` still reads "fail" — filtering on result alone misses them.) |  |
| `send_message_empty` | PASS | run-575 | 230 |  | AxiosError |
| `send_message_offline` | PASS | run-575 | 308 | CONFIRMED FIX, STRENGTHENED. The cleanest claim in the campaign: 0/9 pass across every OLDER sha, then 6a19ce7 -> sha 5cfa980e2335 -> 2/2 pass (run-526, run-533). No ambiguity, no stale-evidence caveat, and the two passes are in different cycles on the 360dp device. This is the shape every other fix claim should be measured against. | hideKeyboard is Back on Android and popped the conversation; "Send" was on another screen. |
| `send_message_whitespace` | PASS | run-575 | 359 |  |  |
| `send_multiple_messages` | PASS | run-575 | 373 |  |  |
| `send_photo` | PASS | run-575 | 325 | flow | Asserted "common.close" — a t() KEY copied from a Jest test. |
| `start_conversation` | FAIL-assert | run-575 | 330 | SESSION-BLEED (not fixed — root cause is login.yaml). TWELFTH flow in the family, and NOT a new shape: checked against the five named patterns before assuming. Failed '"Contact Seller" is visible', an owner-gated affordance. Identical 12-node signature: listing detail present (save-toggle-button, more-options-button), sticky action row EMPTY — the Ban pill, which ListingDetail.tsx:1199 makes exclusively the isOwnListing branch. Jacket 5060 again: active, negotiable, owned by 594, while the flow expects BUYER affordances. | 2026-09-15: surfaced by qa/lib/live_tally.py, which separates live failures from stale rows — it was missing from my own manual count of run-575's fails. Family now TWELVE flows on one helper. |
| `start_conversation_and_reply` | PASS | run-575 | 321 |  | Parsing Failed at /home/hama99o/Apps/Personal/Hatiwal/hatiwal-mobile/maestro/_helpers/open_bundle.yaml:216:41 |
| `unread_badge_survives_navigation` | FAIL-assert | run-575 | 444 | FLOW-BUG-FIXED bf3303bc2371 — TAPPED THE MODAL TO OPEN THE MODAL. 0/15 on "Element not found: Id matching regex: conversation-action-menu". That id is the MODAL ITSELF (ConversationRow.tsx:404, `<Modal visible={menuVisible} testID="conversation-action-menu">`), so it exists only after something opens it. The opener is the kebab at :396, `conversation-options-${item.id}`. run-573's hierarchy shows SIX kebabs on screen (conversation-options-2863 … -2861) and no action-menu node at all — a closed modal. Changed the tap to conversation-options, matching mark_read.yaml and make_phone_case_unread.yaml. NEEDS A DEVICE RUN at sha bf3303bc2371. | 2026-09-15: NOT a stale selector — conversation-action-menu is live and correct, and mark_read.yaml uses it properly as a VISIBILITY GUARD to detect an already-open menu and `back` out of it. Same id, two roles; only the TAP was wrong. Swept the suite for other tap-the-modal sites: zero remaining, this was the only one. |
| `view_other_profile_from_conversation` | PASS | run-575 | 248 | flow | "Member since" is own-profile only (Profile.tsx); public profile shows a "Joined" tile. |

## `listings` — Seller create/edit/delete + the 3-state lifecycle (Draft/Live/Sold) — Mark sold is always the one-tap primary, no Reserved tab

25/41 passing · 9 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `create_listing` | PASS | run-574 | 218 | PASS (run-525, 275s) — and it is the reference: it scrolls to each field before tapping, which is exactly what its three failing siblings were missing. |  |
| `create_listing_all_fields` | PASS | run-574 | 329 | flow — the description scroll WORKED and the failure MOVED one field. run-525 died on `listing-form-description-input`; run-528 dies on `listing-form-price-input`, which is a different field and a different direction. Price sits ABOVE description in the form (ListingForm.tsx: price ~1400, description ~1785), so scrolling DOWN to the description leaves price above the viewport, and the price `tapOn` had no scroll at all — `tapOn` never scrolls. Added scrollUntilVisible direction UP with centerElement. Still 0/7, but one field further along. | Leftover map steps opened the map, breaking set_listing_location's own scroll; helper does it. |
| `create_listing_category_search` | PASS | run-559 | 142 | flow — FIXED. Typed "Elect" then asserted "Electronics" after only a waitForAnimationToEnd, but FILTERING a category list is a data operation, not an animation. "Electronics" is not stale copy either — it comes from the API's categories, not i18n. Converted to extendedWaitUntil (15s). |  |
| `create_listing_currency_eur` | PASS | run-559 | 169 | run-535 FAIL, but NOT caused by the PKR feature work — checked, because adding a currency row to the picker is exactly the kind of change that silently shifts an index-based selector. It does not here: ListingForm.tsx:1962 builds the id as a TEMPLATE LITERAL, testID={`listing-form-currency-${opt.value}`}, so it is VALUE-derived. `listing-form-currency-EUR` and `-USD` resolve exactly as before and the new `-PKR` row disturbs nothing. (A literal grep for the id finds nothing — this is the template-literal selector trap.) Record is 2/4 at the current sha (run-528 pass, run-535 fail, history mixed): FLAKY, and flaky BEFORE the PKR change. Do not patch. | My Shop list is virtualised, so an unrendered card is absent; now searches. Price is one node (€250.00). |
| `create_listing_currency_pkr` | PASS | run-559 | 127 |  |  |
| `create_listing_currency_usd` | PASS | run-559 | 190 | run-535 FAIL, but NOT caused by the PKR feature work — checked, because adding a currency row to the picker is exactly the kind of change that silently shifts an index-based selector. It does not here: ListingForm.tsx:1962 builds the id as a TEMPLATE LITERAL, testID={`listing-form-currency-${opt.value}`}, so it is VALUE-derived. `listing-form-currency-EUR` and `-USD` resolve exactly as before and the new `-PKR` row disturbs nothing. (A literal grep for the id finds nothing — this is the template-literal selector trap.) Record is 2/4 at the current sha (run-528 pass, run-535 fail, history mixed): FLAKY, and flaky BEFORE the PKR change. Do not patch. | Asserted "$450" — `$` is a regex end-anchor, so it could never match. |
| `create_listing_draft_discard` | FAIL-assert | run-559 | 147 |  | [Failed] create_listing_draft_discard (2m 18s) (Assertion is false: "Discard changes?" is visible) |
| `create_listing_draft_restore` | FAIL-redbox | run-559 | 207 | APP-BUG | ROOT CAUSE FOUND 2026-09-15, 0/17 and nobody had looked. NOT a stale selector — `listing-form-price-input` and `browse-tab` both resolve (the latter is DYNAMIC, `${route.name}-tab`, so a literal grep says 0 and lies). The run-559 screenshot shows a FULL-SCREEN LogBox over the form: `MapLibre Native [ERROR] [ReactTagResolver] reactTag 2492 resolved to view null which is null or a wrong type`. The form renders fine underneath; Maestro cannot see it because an error overlay owns the screen. logcat puts it next to `permissioncontroller` activity, i.e. the map view is torn down under a permission dialog while MapLibre still holds its React tag. Reported to the owner rather than patched — this is app lifecycle, not flow text. NOTE LogBox is dev-only, so a release build would not show the overlay; the underlying lifecycle error is still real. |
| `create_listing_full_publish` | PASS | run-559 | 202 |  | AxiosError |
| `create_listing_multi_quantity` | PASS | run-559 | 210 | flow (heading off screen) — the flow taps the inert "Create Listing" HEADING to blur a field, which is the right trick: `hideKeyboard` is BACK on Android and a dirty form intercepts BACK as "Discard changes?" (run-034). But the heading sits INSIDE the scroll view, not in a fixed header, so once the form has scrolled and the keypad covers the lower half it is off the top — and `tapOn` never scrolls. run-528 died at step-75 on `Element not found: Text matching regex: Create Listing`, a heading that is present and correct. Added scrollUntilVisible direction UP before BOTH heading taps. Kept the tap, not hideKeyboard. | Found UI-011 (HIGH): quantity never reached the API on create/edit — typed 15, stored 1, because both multipart builders are field-by-field allow-lists that never appended it. Also UI-012: the toggle row's label was inert (only the 44x24 switch responded) and the shared Switch had no testID, so no flow could target any switch in the app. Flow needed: a leaf category (Electronics is a parent and leaves the picker over the form), no hide-keyboard on a dirty form (Android BACK → "Discard changes?"), and Save Draft tapped in the fixed toolbar rather than after a keyboard dance. run-042 green; DB confirms qty=15 multi=true. |
| `create_listing_price_edges` | PASS | run-559 | 161 |  |  |
| `create_listing_publish_blocked` | FAIL-assert ⟳stale | run-559 | 232 | FLOW-BUG-FIXED cf6438eeef6a — LOST THE TAB BAR. Failed on "Element not found: Id matching regex: browse-tab" at step 120 of 121. Dismissing the publish success sheet with Done leaves the seller on their OWN OWNER DETAIL, which has no tab bar, so the whole of scenario 2 never ran. Added pop_to_tab_bar (17 other flows already do). NEEDS A DEVICE RUN at sha cf6438eeef6a. | 2026-09-15 RETRACTION: I had this recorded as a suspected publish-validation BYPASS. It is NOT. commands.json shows steps 116-119 COMPLETED — "Your listing is live!" and the title both asserted, after a photo was added. The photoless block WORKED; the flow just could not navigate afterwards, and the "Active" listing in the screenshot is the one published legitimately at step 114. Also extended audit_structure: PUSHERS is matched by testID and selector_id() returns None for text, so a screen entered by tapping a LABEL was invisible to Check 3 — which is why the audit reported clean on this exact defect. |
| `create_listing_publish_direct` | PASS | run-559 | 215 |  |  |
| `create_listing_publish_requirements` | PASS | run-559 | 157 |  |  |
| `create_listing_quantity_edges` | FAIL-assert ⟳stale | run-559 | 182 | FLOW-BUG-FIXED 6a5e082bcc1c — ASSERTED FROM THE TOP, KEYBOARD UP. Failed on '"Enter a number between 1 and 999." is visible'. The scroll above deliberately goes UP to "Create Listing" (proving Save Draft kept the seller on the form), which parks the viewport at the top while the error renders at the quantity field. run-559's hierarchy shows Photos and Title on screen, quantity nowhere — AND the IME up, y=770-1184 of a 1280 screen. Added hideKeyboard + a scroll to the message. Fifth instance of the scroll-past shape. NEEDS A DEVICE RUN at sha 6a5e082bcc1c. | 2026-09-15: not an app bug as far as the evidence goes — MAX_LISTING_QUANTITY=999 and zod has `.max(MAX_LISTING_QUANTITY)`, onSaveDraft is a handleSubmit so it does validate, and isFormBlocking is only about edit/duplicate loading so the button was not disabled. The fix lets the flow actually SEE the field; if the message is still absent after scrolling to it, THAT is the app bug and this row should say so. |
| `create_listing_title_edges` | PASS | run-559 | 175 | env | Login gate timed out at 60s under host load; flow never ran its own steps. |
| `create_listing_validation` | PASS | run-559 | 148 |  |  |
| `create_listing_with_condition` | PASS | run-559 | 359 | PASS in run-528 (517s) at sha 55325c0659f3 — UNCHANGED since August, so this is NOT a campaign fix and is not claimable. Its four prior failures span three different kinds (app_bug_or_flow, rig_fail at 602s, app_error), i.e. environment-sensitive rather than stably broken. Now 1/4 once the rig_fail row is excluded. A reminder that STABLE FAIL is provisional: 0/4 can become 1/5 with no edit at all. | Tapped a title sitting in the search box, so the tap hit the input. Card testID now. |
| `create_listing_with_photos` | PASS | run-559 | 223 |  |  |
| `delete_listing` | PASS | run-559 | 166 | flow (IME covers the field below the title) — identical shape and identical fix to create_listing_currency_eur, this campaign's CONFIRMED fix (66c3093: five consecutive failures at sha 198aa2244b02, then run-528 PASS). The flow types the title, which raises the IME over roughly half the window at 360dp, then taps a field below it — and `tapOn` never scrolls. The testID is CURRENT. Added the proven scroll: `scrollUntilVisible` DOWN, timeout 20000, and deliberately NO centerElement (a field near the end of a form cannot be centred). Failed on `listing-form-price-input`. | Toast unwaitable: onDeleted does router.replace, so it fires on a dying screen. Asserts the outcome. |
| `draft_lifecycle` | FAIL-assert ⟳stale | run-559 | 217 | FLOW-BUG-FIXED 7279a69379dc — WATCHED FOR THE WRONG ELEMENT. Failed on '"Your listing is live!" is visible'. That is PublishSuccessSheet, which MyListingDetail opens ONLY when the route param published=1 arrives — set once by ListingForm after a publish from the CREATE FORM (MyListingDetail.tsx:97-117). Publishing an existing draft IN PLACE never re-navigates, so the sheet correctly never opens. What fires is useListingLifecycle.ts:252, toast.success(listing.publishSuccess) = "Listing published!". The publish had SUCCEEDED every time. NEEDS A DEVICE RUN at sha 7279a69379dc. | 2026-09-15: the two keys differ by one path segment — `listing.publishSuccess` (toast) vs `listing.form.publishSuccess.title` (sheet) — which is how the wrong one got written down; lifecycle_publish already asserts the toast, via extendedWaitUntil because a toast lives ~3s. SECOND defect in the same flow: it then tapped browse-tab from the owner detail, same missing pop as create_listing_publish_blocked. audit_structure still cannot see this one — the screen is entered by `tapOn: "Lifecycle Test Item"`, a TEXT selector, and widening PUSHERS to every text tap would drown the check. |
| `edit_listing` | PASS | run-559 | 182 | flow (IME covers the field below the title) — identical shape and identical fix to create_listing_currency_eur, this campaign's CONFIRMED fix (66c3093: five consecutive failures at sha 198aa2244b02, then run-528 PASS). The flow types the title, which raises the IME over roughly half the window at 360dp, then taps a field below it — and `tapOn` never scrolls. The testID is CURRENT. Added the proven scroll: `scrollUntilVisible` DOWN, timeout 20000, and deliberately NO centerElement (a field near the end of a form cannot be centred). Failed on `listing-form-price-input`. | **2026-09-13 21:05 — THIS RESULT IS VOID, NOT A VERDICT.** It ran against a broken Metro bundle: adding `src/i18n/locales/ur/` created a NEW DIRECTORY, which Metro does not register until restarted, so every flow on both emulators red-screened with UnableToResolveError. Fixed by `docker restart hatiwal-mobile-mobile-1`. Do not triage this run — re-judge on the next clean pass. See QA_HANDBOOK.md. |
| `edit_listing_all_fields` | FAIL-assert ⟳stale | run-559 | 168 | flow-bug | [Failed] edit_listing_all_fields (2m 39s) (Element not found: Id matching regex: listing-form-price-input) |
| `edit_listing_discard` | FAIL-assert | run-559 | 152 | STALE — run-252 executed the old route (commands.json proves it); already fixed | Same detail-control-from-the-list mistake; identical opening in four flows. |
| `edit_listing_quantity` | FAIL-assert | run-559 | 171 |  | [Failed] edit_listing_quantity (2m 43s) (Element not found: Id matching regex: listing-form-price-input) |
| `edit_listing_remove_photo` | FAIL-assert | run-559 | 153 | flow (arbitrary card, not a fixture) — NOT the off-screen family, and the app was RIGHT. It opened the FIRST `seller-listing-card`, which in run-528 was listing 3317 "Laptop HP": status DRAFT with exactly ONE photo. On a draft the toolbar is [Save Draft | Publish], never the single [Save] this flow taps (`isPublished = existingListing.status !== "draft"`, ListingForm.tsx:451), and removing the only photo empties the gallery — the end-of-flow screenshot is precisely that toolbar over an empty Photos section. Card order is not a fixture: the DB is re-seeded between cycles (the seller's id moved 420 -> 423 -> 426 inside a few hours). Converted to search_my_shop for "School Backpack Bulk Restock - 20 Bags" — ACTIVE with THREE photos (API-checked) and referenced by NO other flow, so mutating its photos cannot disturb another fixture, unlike the Lenovo ThinkPad which the seeder uses as the price-drop target. |
| `edit_listing_reorder_photos` | FAIL-assert | run-559 | 152 | flow (arbitrary card, not a fixture) — NOT the off-screen family, and the app was RIGHT. It opened the FIRST `seller-listing-card`, which in run-528 was listing 3317 "Laptop HP": status DRAFT with exactly ONE photo. On a draft the toolbar is [Save Draft | Publish], never the single [Save] this flow taps (`isPublished = existingListing.status !== "draft"`, ListingForm.tsx:451), and removing the only photo empties the gallery — the end-of-flow screenshot is precisely that toolbar over an empty Photos section. Card order is not a fixture: the DB is re-seeded between cycles (the seller's id moved 420 -> 423 -> 426 inside a few hours). Converted to search_my_shop for "School Backpack Bulk Restock - 20 Bags" — ACTIVE with THREE photos (API-checked) and referenced by NO other flow, so mutating its photos cannot disturb another fixture, unlike the Lenovo ThinkPad which the seeder uses as the price-drop target. |
| `expired_listing_badge` | PASS | run-559 | 136 | flow (below the fold) — the fixture and the app are BOTH right. The API says listing 3279 "Expired Winter Coat Size L" is expired=true (expires_at 2026-09-10), and the `assertVisible: "Expired"` two lines above PASSED, so the flow is on the right listing. "Renew" is the PRIMARY ACTION label on the owner detail's Manage Listing row (useListingLifecycle.ts:475 — the same button that reads "Publish" for a draft and "Mark as Sold" for a live listing), which sits below the gallery, price, analytics and the map. assertVisible means ON SCREEN. Added scrollUntilVisible DOWN. | No expired listing existed at all; expires_at was never seeded. Fixture added. |
| `lifecycle_publish` | FAIL-assert | run-559 | 158 | fixture | Draft tab index 0 was a photoless QA draft, so publish was blocked. Seeded "Ready To Publish Draft". |
| `lifecycle_reactivate` | PASS | run-559 | 179 | flow | Detail control with the card sheet already open; sheet offers listing-action-activate. |
| `lifecycle_reserve` | FAIL-assert | run-559 | 195 | SESSION-BLEED (not fixed — root cause is login.yaml). Failed on "Contact Seller", an owner-gated affordance. Same 12-node signature as the four offer flows: listing detail present (save-toggle-button, more-options-button) with the sticky action row EMPTY — the Ban pill, which ListingDetail.tsx:1199 makes exclusively the `isOwnListing` branch. Listing 5074 is status=active, negotiable=true, owned by 594 (seller@hatiwal.test), and the flow reaches this step expecting BUYER affordances. canContactListing returns false only for an owner on an active listing, so the app was running as 594. | 2026-09-15: login.yaml NAMES this flow in its own comment — "reserved_buyer, listing_conversations and reserve_after_accept each log in via THIS helper as the buyer, open a seller-owned listing, and fail on 'Make an Offer' not existing." The helper documents the symptom of its own missing guard. Part of a family now at EIGHT flows: 4 offer flows, meetup_respond, meetup_full_cycle, and these two. No flow-level patch is appropriate — the fix is the guard in login.yaml, which needs a device. |
| `lifecycle_sold` | PASS | run-559 | 146 |  |  |
| `lifecycle_unpublish` | PASS | run-559 | 147 |  |  |
| `listing_analytics_sparkline` | FAIL-assert | run-559 | 142 | TRIAGED, not yet fixed. Fails at step-69 on `"No views yet in the last 7 days" is visible` — analytics sits well down the owner detail (seen below the fold in publish_from_owner_detail's screenshot), so this is probably the below-the-fold case, but it needs its own evidence. | Analytics does not render for a draft ({!isDraft}); needed the Active tab, not just a scroll. |
| `listing_conversations_list` | PASS | run-559 | 127 | flow | Tapped "chats"; the card renders "{{count}} chats". |
| `listing_renew_flow` | FAIL-assert ⟳stale | run-559 | 141 | FLOW-BUG-FIXED d3ff957e8d0d — RENEW IS BELOW THE FOLD. 0/12 on '"Renew" is visible'. "Renew" is the lifecycle-primary-action's LABEL for an expired listing — the same button that reads Publish on a draft and Mark as Sold on a live one — and it renders below the photo, description and views chart, while the detail opens at the TOP. run-559's hierarchy is that top: badge, "Expired", price, "Expired Winter Coat Size L", "Posted September 14, 2026", Views/Chats, "Views — last 7 days". Both the assert and the tap were bare. NEEDS A DEVICE RUN at sha d3ff957e8d0d. | 2026-09-15: one scroll serves both the assert and the tap — nothing between them navigates, so the offset carries. Same shape as my_listing_detail_view and the rest of the occlusion class. |
| `listing_status_counts` | PASS | run-559 | 137 | TRIAGED, not yet fixed. Fails at step-79 on `"All" is visible`. NOT the tab-bar case — run-528's sibling screenshot shows the filter row rendering "All 40 / Draft 9 / Active 27 / Expired 1" normally, so the chips exist. Needs its own screenshot before a fix. | "Sold" is the last tab in a horizontal scroller; scrollUntilVisible swipes at screen centre. |
| `my_listing_detail_view` | FAIL-assert ⟳stale | run-559 | 143 | FLOW-BUG-FIXED 5667c41c13ab — FOUR COMMANDS SHARING ONE SCROLL. 0/12 on 'id: lifecycle-primary-action is visible'. The flow scrolled DOWN to analytics-views once, then asserted three progressively LOWER elements and later one ABOVE, all bare. run-559's hierarchy is the analytics block — status badge, "Expires in 30 days", price, title, "Posted September 15, 2026", Views/Chats, "Views — last 7 days" — with the action row nowhere on screen. Each command now carries its own scroll, in the direction its target actually lies. NEEDS A DEVICE RUN at sha 5667c41c13ab. | 2026-09-15: the trap was that adding ONE scroll would have traded this failure for another — the later `assertVisible analytics-views` (after returning from the conversations screen) sits ABOVE the action row, so scrolling down for step 13 would have broken step 21. Read the whole flow before patching. draft_lifecycle reaches the same button with the same scroll and passes. |
| `my_listings_filter_tabs` | PASS | run-559 | 134 | flow — same; executed step was pre-swipe scrollUntilVisible. Verdict stale | Same clipped last tab; coordinate swipe across the row. |
| `my_listings_search` | PASS | run-559 | 136 | flow (the IME covers the TAB BAR) — a fifth face of "rendered but not on screen", and the first that is not a form field. "My Shop" is the BOTTOM TAB's label (sidebar.myListings — the string does not exist anywhere else in the app; src/ mentions it only in comments and a Storybook story). It fails at the SECOND of the flow's two identical asserts, after `eraseText` clears the search — eraseText does NOT dismiss the keyboard, so the tab bar stays covered. run-528's screenshot is that screen: search focused, keypad over the lower half, one card visible. Fixed with `pressKey: Enter` (never hideKeyboard, which is BACK on Android). Checked the spread first: exactly one flow asserts a tab label after typing. | Asserted a bare "No"; now asserts the absence of cards instead of empty-state copy. |
| `price_drop_after_edit` | FAIL-assert ⟳stale | run-559 | 169 | FLOW-BUG-FIXED 2d94efa8ac42 — BADGE ABOVE, PAGE AT THE BOTTOM. 0/12 on '"Active" is visible' after saving an edit. The owner detail returns at whatever offset it was left at, and run-559's hierarchy is the BOTTOM of it: "10th District, Kabul", "Get Directions", "Manage Listing", "Mark as Sold", "View Conversations". listing-status-badge is MyListingDetail.tsx:336 of 705 lines, well above all of that. Scrolls UP to it now. NEEDS A DEVICE RUN at sha 2d94efa8ac42. | 2026-09-15: scrolled to the BADGE's testID rather than to the word — "Active" is ambiguous on this screen family (an "Active Listings" stat carries it), while listing-status-badge is the element whose status this step actually checks. Same mechanic as user_profile_sold_tab in the opposite direction. |

## `seller` — One-tap Mark sold from any live listing (never reserve-first) + the Sales ledger (edit/void a row, reviewed-sale refusal, outside-buyer rows, undo-after-sold)

7/18 passing · 9 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `held_quantity_refusal` | FAIL-assert | run-534 | 258 | FLOW-BUG-FIXED 72c7ac0adeaf — SCROLLED TO A LANDMARK, NOT THE TARGET. Failed 'id: stock-badge-owner is visible'. It scrolled UP to the listing TITLE then asserted the badge; scrollUntilVisible stops the instant its target appears, parking the title at the BOTTOM of the viewport with everything above it off screen — and the badge renders at MyListingDetail.tsx:357, ABOVE the price and title. run-576's hierarchy is exactly that: "AFN 350", "each", title, location, "Posted September 15 2026", Views/Chats, "Views — last 7 days" — scrolled down to analytics-views (:405). Now scrolls to stock-badge-owner itself. NEEDS A DEVICE RUN at sha 72c7ac0adeaf. | 2026-09-15: same trap and same fix as change_language_pashto and view_profile — scroll to the element you are about to assert, never to a landmark near it. NOTE this run also CONFIRMED the search_my_shop helper fix (2681910) is live: run-576 executed at 15:53, after the 14:20 commit, and its commands.json contains pressKeyCommand. The flow got past the card actions and failed later, which is the helper fix working. |
| `listing_actions_sheet` | FAIL-assert | run-534 | 223 | FLOW-BUG-FIXED 75e4800294bd — SECOND covered tap in the same flow. run-576 failed EARLIER than before, on "Element not found: Text matching regex: Electronics". The picker never opened: the hierarchy is the create form with "Tap to select category" STILL on it and keyboard_holder mounted. The price is typed two steps up, so the IME covered the row; Maestro reports a covered tap COMPLETED, and the flow died on a category that only exists INSIDE the picker. The failure named a category; the cause was a keyboard. Added the guarded dismissal. NEEDS A DEVICE RUN at sha 75e4800294bd. | 2026-09-15: the earlier set_listing_location fix is NOT disproven by this run — the flow never reached line 127, so commands.json contains no location-confirm and no keyboard_holder guard at all. Two covered taps, one flow, the category one earlier than the location one. NB the form shows price "50003" where the flow types "5000": eraseText/inputText interleaving, the shape search_my_shop.yaml documents. Not chased — it does not affect this failure, but it is a real defect in this flow worth its own look. |
| `listing_conversations` | FAIL-assert | run-534 | 218 | SESSION-BLEED (not fixed — root cause is login.yaml). ELEVENTH flow in the family. Failed '"Make an Offer" is visible'. Identical 12-node signature to the rest: listing detail present (save-toggle-button, more-options-button), sticky action row EMPTY — the Ban pill, which ListingDetail.tsx:1199 makes exclusively the isOwnListing branch. The listing is jacket 5060, active and negotiable and owned by 594, while the flow reaches this step expecting BUYER affordances. | 2026-09-15: found by qa/lib/audit_helpers.py clustering callers of pop_to_tab_bar. login.yaml names THREE flows in its own comment — reserved_buyer, listing_conversations and reserve_after_accept — and all three are now confirmed, which is as direct a confirmation of that comment as the suite can give. Family: 4 offer flows, meetup_respond, meetup_full_cycle, reserved_buyer, lifecycle_reserve, reserve_after_accept, reserve_after_buyer_accepts_counter, listing_conversations. |
| `mark_sold_all_units` | PASS | run-534 | 209 | flow — FIXED. The fixture was NOT missing: checked against the API, the seller has 36 listings with 11 "QA Disposable ..." among them and "QA Disposable quantity_all" is present and ACTIVE. The flow was already guarded (extendedWaitUntil on seller-listing-card), so this was a scrollUntilVisible running out of TIME across 36 cards at 20s — the same race the Bazaar feed lost. Converted to _helpers/search_my_shop.yaml (QUERY "quantity_all") + wait + tap; search is O(1) in list size. |  |
| `mark_sold_with_buyer` | PASS | run-534 | 202 | CONTENTION-SUSPECT — DO NOT PATCH ON ONE RUN. Failed run-576 on '"How was Ahmad Karimi as a buyer?" is visible' after passing FIVE times at this same sha (66a1ef8aa2f7). commands.json: step 87 is the assertNotVisible and COMPLETED (correct — the prompt opens AFTER the toast's ~4s lifecycle), step 88 is the extendedWaitUntil with a 10s budget and FAILED. So the sheet never appeared in time. It ran at 16:12 with the box at load 27. | 2026-09-15: EXONERATES my search_my_shop helper change, which was the first suspect because this flow is a caller and was passing before it. It is not the cause: the executed record shows the flow completed the search, the buyer picker, the confirm, the Undo toast and 80+ commands, failing only on a timing wait right at the end. A keyboard dismissal at step ~20 cannot produce that. Per the handbook's flakiness rule a single run is not evidence — needs a repeat on a quiet box before anyone touches the 10s budget. |
| `multi_quantity_offplatform_sale` | PASS | run-534 | 253 | flow — FIXED, same cause and same fix as mark_sold_all_units. "QA Disposable offplatform_units" verified present and ACTIVE via the API; converted to search_my_shop.yaml (QUERY "offplatform_units"). |  |
| `multi_quantity_partial_sale` | FAIL-assert | run-534 | 284 | flow — FIXED. Bare assertVisible on "The price for one item" inside the buyer-picker sheet, which fills in progressively — the quantity control asserted just above can be present before the hint is. Copy is CURRENT (buyerPicker.json `finalPricePerUnitHint`). Converted to extendedWaitUntil (15s). | Found UI-008 (HIGH): typed 3, sold all 15 — pre-filled field appended, clamp silently swallowed it, listing retired. Fixed with selectTextOnFocus + a destructive over-stock hint; same fix applied to the web dialog. Also UI-009 ("15 of 15 left" before any sale). Flow itself needed: explicit seller login (login_seller.yaml lands in the dev-client launcher; login.yaml ignores an EMAIL override when a session exists), scrollUntilVisible on `lifecycle-more-action`, and the review prompt instead of the racing toast. run-020 green, 0 api errors, DB confirms 3 sold / 12 left / still active. |
| `publish_from_owner_detail` | PASS | run-534 | 372 | STRONG FIRST RESULT, not yet a verdict. 0/15 pass across EVERY older sha — it had never passed once — then 0260df8 -> sha adecc87e0eb2 -> run-534 PASS at 372s. 1/1. By the standard this campaign holds itself to (send_message_offline is 2/2 against 0/9), one run is not a verdict, so this stays a claim until a second cycle confirms it. But 0/15 -> pass is a materially different signal from the 0/1 -> fail results elsewhere. The fix was a KEPT SCROLL, not the dialog: the owner detail retains the scroll position the flow left it at, so the status badge and title sit above the viewport and assertVisible means ON SCREEN. RE-CHECK NEXT CYCLE. |  |
| `publish_success` | PASS | run-534 | 441 | First data at sha a78b23ed2816 (0260df8) is 1/1 PASS at 441s. WEAKER than its sibling: this flow had 1/16 at older shas, so it has passed before and a single pass does not distinguish the fix from that background rate. Needs several more runs. Same root cause and same commit as publish_from_owner_detail (kept scroll on the owner detail). | 2026-09-05 title asserted while the detail screen was scrolled past it; guarded UP scroll added. |
| `reserved_buyer` | FAIL-assert | run-534 | 376 | SESSION-BLEED (not fixed — root cause is login.yaml). Failed on "Make an Offer", an owner-gated affordance. Same 12-node signature as the four offer flows: listing detail present (save-toggle-button, more-options-button) with the sticky action row EMPTY — the Ban pill, which ListingDetail.tsx:1199 makes exclusively the `isOwnListing` branch. Listing 5078 is status=active, negotiable=true, owned by 594 (seller@hatiwal.test), and the flow reaches this step expecting BUYER affordances. canContactListing returns false only for an owner on an active listing, so the app was running as 594. | 2026-09-15: login.yaml NAMES this flow in its own comment — "reserved_buyer, listing_conversations and reserve_after_accept each log in via THIS helper as the buyer, open a seller-owned listing, and fail on 'Make an Offer' not existing." The helper documents the symptom of its own missing guard. Part of a family now at EIGHT flows: 4 offer flows, meetup_respond, meetup_full_cycle, and these two. No flow-level patch is appropriate — the fix is the guard in login.yaml, which needs a device. |
| `sales_screen_correct_quantity` | PASS | run-534 | 576 | run-534 PASS at 576s, but this is FLAKY not fixed: 3/10 at the current sha 85a34f4bc624 (failed 497, 501, 505, 516, 524, 527) against 0/4 at older shas. Today's pass is part of the existing 30% rate — do NOT read it as a verdict and do not patch it. |  |
| `sales_screen_reviewed_sale_refusal` | FAIL-assert | run-534 | 326 | flow — `seller-card-more-action` not found; the testID IS current (SellerListingCard.tsx:464), so this is a reach/timing failure, not selector rot. Ran AFTER the identity fix and shows no wrong-account signature. | [Failed] sales_screen_reviewed_sale_refusal (4m 42s) (Element not found: Id matching regex: seller-card-more-a |
| `sales_screen_void_row` | FAIL-assert | run-534 | 499 |  | [Failed] sales_screen_void_row (7m 14s) (Element not found: Id matching regex: listing-form-quantity-switch) |
| `save_draft` | (rig) | run-534 | 603 | flow (heading inside the scroll view) — died at step-107 on the LATE `"Create Listing"` assert (line 178). The file has four occurrences including an assertNotVisible, so the message alone could not say which; the screenshot settled it — the form is scrolled to Title/Price showing "Title is required (max 150 characters)" with the numeric keypad up, which is the validation section at line 178, not the fresh form at line 122. Added scrollUntilVisible UP. Fourth flow of this face. |  |
| `sell_without_reserving` | (rig) | run-534 | 602 |  |  |
| `sold_quantity_reconciliation` | FAIL-assert | run-534 | 572 | flow (IME covers the field) — the app and the fixture are BOTH correct, checked before touching the flow. `showQuantityReopenNote = hasMultipleUnits && willReopenOnSave(...)` (ListingForm.tsx:481) needs a SOLD listing, and the API says listing 3274 "QA SF-M7 Reconcile Batch" is status=sold, quantity=5, sales_count=1 — so typing 8 yields exactly the "3 available" the next line asserts. The note IS rendered; it is simply not ON SCREEN. Typing into the quantity field raises the NUMERIC KEYPAD and the form ends up back at the top — run-527's screenshot is this form showing Photos and the Title with the keypad covering everything below, and the note sits directly under the quantity field. Added scrollUntilVisible DOWN with centerElement so it lands ABOVE the keypad (not hideKeyboard, which is Back on Android and would pop the form). | [Failed] sold_quantity_reconciliation (8m 15s) (Assertion is false: "QA SF-M7 Reconcile Batch" is visible) |
| `undo_mark_sold` | FAIL-assert | run-534 | 219 | HELPER-FIX (set_listing_location.yaml) — IME COVERED THE LOCATION ROW. Failed 'Assertion is false: id: location-confirm is visible', which had nothing to do with the map. run-534's hierarchy shows the CREATE LISTING FORM still on screen (price 50003, category "Phones & Tablets", condition) with keyboard_holder mounted — the picker never opened, because the tap landed on the IME covering the location row and Maestro reports such a tap COMPLETED. Added a GUARDED hideKeyboard to the helper. A helper change leaves this flow's sha UNCHANGED, so judge by run date against the helper's commit, or by commands.json. | 2026-09-15: found by qa/lib/audit_helpers.py, which clusters callers' failures — this pair was the one cluster it surfaced that I had not already found by hand. Guarded on `.*keyboard_holder` rather than firing hideKeyboard unconditionally: it is Android BACK, the helper has 16 callers and 8 pass, and Back with the IME down would take those eight OUT of the form. For listing_actions_sheet this supersedes the earlier 'FOUR PATCHES DECLINED' note — those patches targeted the sheet and the tap; the fault was two steps earlier, in a shared helper. |
| `undo_mark_sold_with_buyer` | PASS | run-534 | 181 |  |  |

## `profile` — Profile view/edit, language + theme switch, stats, blocked users

19/31 passing · 8 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `account_delete_and_restore` | FAIL-assert ⟳stale | run-560 | 147 | TRIAGED, not fixed — 0/8, different cause from its sibling. Fails at step-59 on `tapOn id: register-email-input`, i.e. while trying to register a fresh account, not on the delete control. Needs its own screenshot. | [Failed] account_delete_and_restore (2m 19s) (Element not found: Id matching regex: register-email-input) |
| `account_delete_cancel` | PASS | run-560 | 187 | flow (centerElement on the LAST element) — 0/8, and the cause is one word. `scrollUntilVisible` used `centerElement: true` on "Delete account", which sits at the very BOTTOM of Profile. An element at the END of a scroll container cannot be centred — nothing below it to scroll past — so the constraint is unsatisfiable and the scroll burns its whole timeout on a control that is already on screen. run-529's screenshot shows "Delete account" plainly visible above the tab bar. The copy is current too (profile.json:160). Commit 66c3093 names this exact trap. CONTROL GROUP, and why this is a one-flow change: auth/logout, auth/logout_cancel and auth/login_deep all scroll to "Sign Out" with centerElement and are 4/4 each — Sign Out has "Delete account" below it, so it CAN be centred. Dropped centerElement here only. |  |
| `away_mode` | PASS | run-560 | 176 | app+flow — away row was untappable (no Pressable/testID); fixed cb68fa4 (live via Metro, no rebuild) |  |
| `blocked_users` | PASS | run-560 | 142 |  |  |
| `change_language_dari` | PASS | run-560 | 147 |  |  |
| `change_language_english` | PASS | run-560 | 529 | rig (our own 600s cap), NOT a verdict — and deliberately not fixed. Durations creep: 278, 431, 578, 241, 572, then 601 in run-529, where it was recorded kind=rig_fail. Language flows call reloadApp() (applyLanguageFromUser on a direction change), which is why English is far slower than its Dari sibling at 172-208s. The in-flow marker exists — it runs `_helpers/await_language_restart.yaml` — but 26 flows carry that marker and doubling the cap for all of them to cover one flow's occasional timeout would make every genuinely hung flow burn 20 minutes instead of 10. The cost here is throughput, not correctness: cap timeouts classify as rig_fail and qa.sh flaky already excludes them, so no verdict is harmed. Left alone on purpose. |  |
| `change_language_pashto` | PASS | run-560 | 148 |  |  |
| `change_language_urdu` | UNTESTED | — |  |  |  |
| `contact_visibility` | FAIL-assert | run-560 | 483 | 3/9 — FLAKY, so NOT a verdict target, and its own comment already names the cause: `edit-profile-whatsapp-same-as-phone` renders only while a phone exists AND the WhatsApp value DIFFERS from it, so a previous run that SAVED the matching value makes the control correctly hide itself. Self-interference across runs, not a selector problem — the flow already carries an eraseText mitigation and it still recurred in run-529 (step-107, the scroll ran its full timeout). Needs the fixture reset to be made reliable, not another wait. | 2026-09-03: the failing assertion named the copied number but the cause was navigation. hideKeyboard is a Back press and popped Edit Profile to Profile; the next THREE commands reported COMPLETED against a stale hierarchy, so the assertNotVisible before it passed for the WRONG reason. Replaced with pressKey:Enter, which turned out to SUBMIT the form — both removed. Green at 360dp once the keypress was gone; now unstable again from my keyboardDismissMode=on-drag reflowing the form mid-scroll (board #313). NOT an app bug. |
| `edit_profile` | PASS | run-560 | 139 | stale — toast assertion already replaced by durable name check |  |
| `edit_profile_all_fields` | PASS | run-560 | 220 | flow (bio below the fold) — 0/6, and the two asserts ABOVE the failing one are the diagnosis: `"UpdatedFirst"` and `"UpdatedLast"` PASS because First and Last Name sit at the TOP of the re-opened edit form, then `"I sell quality items in Kabul."` fails because the Bio field is further down. assertVisible means ON SCREEN. Died at step-126 in run-529. Added scrollUntilVisible to `edit-profile-bio-input` by id (stable) before asserting the bio TEXT (the thing under test), direction DOWN, no centerElement. | 2026-09-03: FOUR causes, two of them app bugs. (1) asserted text 'Save' on a button reading 'Save Changes'; (2) centerElement on the sticky save button; (3) APP — the sticky Save sat BEHIND the keyboard (d8edc9e), verified visually at 360dp; (4) APP — the keyboard swallowed the tap on the NEXT field, so 'UpdatedLast' landed in the First Name box (e36a6b4, keyboardDismissMode=on-drag). Also a pre-existing viewport assumption on the final derived-city assertion (no scroll). MY OWN regressions along the way: a pressKey:Enter that SUBMITTED the form (608ddda, reverted) and a scrollUntilVisible that is a no-op when the target is already 'visible'. Flow-side stability still open — board #313. |
| `edit_profile_avatar` | PASS | run-560 | 151 |  |  |
| `edit_profile_bio_too_long` | PASS | run-560 | 234 | flow — 520 chars do type; error renders above viewport; now scrolls UP cb68fa4 |  |
| `edit_profile_province` | PASS | run-560 | 224 | flow | 2026-09-02: DOWN + centerElement:true on profile-edit-button, which sits near the TOP of Profile — DOWN scrolls away from it and centring is impossible with too little content above. ORDER-DEPENDENT (siblings passed on the identical block). Now UP + visibilityPercentage 40, applied to all 8 flows carrying it. |
| `edit_profile_validation` | PASS | run-560 | 165 |  |  |
| `hidden_listings` | PASS | run-560 | 162 |  |  |
| `language_persists_across_tabs` | PASS | run-560 | 229 |  |  |
| `language_switch_all_screens` | PASS | run-560 | 268 | flow — asserted Profile content while restart left app on feed; reordered cb68fa4 |  |
| `profile_stats_verify` | FAIL-assert | run-560 | 169 | TRIAGED, not fixed — `No visible element found: "Active Listings"` after a guarded scrollUntilVisible with centerElement:false, so NOT the centerElement trap. The copy exists (profile.json:58) but Profile.tsx does not reference `activeListings` at all, and transaction_stats_hidden_when_zero PASSED in the same run — so a conditional-render hypothesis is live and this needs its own screenshot before a fix. | Same hardcoded year. |
| `recently_viewed` | PASS | run-560 | 151 | flow+app — row had no testID; added profile-row-recently-viewed. Fixed 34e713a |  |
| `recently_viewed_empty_state` | PASS | run-560 | 143 |  |  |
| `seller_mode_toggle` | PASS | run-560 | 155 |  |  |
| `theme_switch` | FAIL-assert ⟳stale | run-560 | 202 | FLOW-BUG-FIXED fc0c714 — LOST ITS PLACE AFTER A RESTART. 0/5 at sha 61aac9849467 on "No visible element found: id: theme-option-system". run-560 step 110's hierarchy shows the BROWSE FEED (browse-search-bar, browse-view-grid, listing-card, "AFN 350") — the scroll was hunting a Profile testID on the Bazaar list. A theme switch restarts the app (theme.store.ts:26 -> reloadApp -> RNRestart) and it returns on the INITIAL route. The FIRST restart is followed by goto_profile_tab; the SECOND was not. Added it. NEEDS A DEVICE RUN at sha a42570b1c3ad. | 2026-09-15: the comment on that line asserted the opposite — "this flow happens to want the feed next, so it passed without waiting" — which was true of an older version and is why the missing nav read as deliberate. Rewritten. A suite-wide sweep for restart-then-Profile-selector found one other hit, language_switch_all_screens, which is a FALSE POSITIVE: its step 24 taps 'من', the Profile tab in Farsi, and it passes. |
| `transaction_stats_hidden_when_zero` | PASS | run-560 | 132 |  |  |
| `transaction_stats_own_profile` | FAIL-assert | run-560 | 165 | flow (kept scroll BETWEEN flows) — the app is not restarted between flows, so Profile keeps the position an EARLIER flow left it at. run-529's screenshot is Profile scrolled to the bottom (Activity / Privacy / Sign Out / Delete account) with the mode row off the top, still in dark mode from theme_switch. The control proves the flow is not wrong: seller_mode_toggle has the byte-identical opening and PASSES — it just runs before anything scrolls Profile down. Killed two hypotheses first: the login helper (5 of 6 failures use login.yaml, same as all 5 passing flows) and the renamed fixture (only 1 of 6 asserts a name). Added a GUARDED scrollUntilVisible UP. | [Failed] transaction_stats_own_profile (2m 37s) (Assertion is false: "Items Bought" is visible) |
| `transaction_stats_public_profile` | FAIL-assert | run-560 | 172 | flow — vacuous assertNotVisible on the dead soldItems key; removed | [Failed] transaction_stats_public_profile (2m 44s) (Assertion is false: id: transaction-stats-badge is visible |
| `transaction_stats_seller_own_profile` | FAIL-assert | run-560 | 184 | flow (kept scroll BETWEEN flows) — the app is not restarted between flows, so Profile keeps the position an EARLIER flow left it at. run-529's screenshot is Profile scrolled to the bottom (Activity / Privacy / Sign Out / Delete account) with the mode row off the top, still in dark mode from theme_switch. The control proves the flow is not wrong: seller_mode_toggle has the byte-identical opening and PASSES — it just runs before anything scrolls Profile down. Killed two hypotheses first: the login helper (5 of 6 failures use login.yaml, same as all 5 passing flows) and the renamed fixture (only 1 of 6 asserts a name). Added a GUARDED scrollUntilVisible UP. | [Failed] transaction_stats_seller_own_profile (2m 55s) (Assertion is false: "Sold" is visible) |
| `user_profile_sold_tab` | FAIL-assert ⟳stale | run-560 | 185 | FLOW-BUG-FIXED 42d8d3bdcf6c — TAB ROW BELOW THE FOLD. 0/13 on '"Active" is visible', a bare assert. UserProfile.tsx:357 puts the Active/Sold TabBar inside ListHeaderComponent AFTER <ProfileHeader/>, so on arrival the tabs sit just below the fold. run-560's hierarchy is exactly the header — Fatima Rahimi, Herat, "1 Active Listings", Joined, "Ratings & Reviews" — 19 nodes ending where the tab row would start. assertVisible never scrolls. NEEDS A DEVICE RUN at sha 42d8d3bdcf6c. | 2026-09-15: "Active Listings" and "Active today" are both on that header and NEITHER satisfies the assert — Maestro matches text against the WHOLE node, so "Active" matches only a node whose entire text is "Active", i.e. the tab. Not a false-positive risk, simply not a match. |
| `view_profile` | FAIL-assert ⟳stale | run-560 | 186 | FLOW-BUG-FIXED cb8dd1e — ASSERTED THE TOP AFTER SCROLLING TO THE BOTTOM. 0/5 at sha 25d7e99db10a on '"Edit Profile" is visible'. It was the LAST step, a bare assertVisible after the scroll to Sign Out (Profile.tsx:1197, bottom of a screen taller than a phone). Every "Edit Profile" label is ABOVE it — the avatar at :641 and profile-edit-button in PersonalInfoCard, above Appearance :855 and Language :932. run-560's hierarchy puts the avatar label at bounds [277,137][445,305]. assertVisible never scrolls. Moved into list order. FOURTH instance after 3db2973 / 07bf502 / b3f5a93. NEEDS A DEVICE RUN at sha 1d643b3d484b. | 2026-09-15: ALSO switched from text to id. "Edit Profile" is ambiguous BY DESIGN — accessibilityLabel of both the avatar (opens the photo PICKER) and the settings row (navigates to edit). Profile.tsx:243 records the picker "kept leaking across flows" for this reason. The text assert was satisfied by the avatar, so passing it would have proved the photo picker exists, not the edit affordance. A false green averted. |
| `view_profile_error` | FAIL-assert ⚠2 | run-560 | 168 |  | AxiosError |
| `view_seller_profile_from_profile` | FAIL-assert | run-560 | 164 | flow (kept scroll BETWEEN flows) — the app is not restarted between flows, so Profile keeps the position an EARLIER flow left it at. run-529's screenshot is Profile scrolled to the bottom (Activity / Privacy / Sign Out / Delete account) with the mode row off the top, still in dark mode from theme_switch. The control proves the flow is not wrong: seller_mode_toggle has the byte-identical opening and PASSES — it just runs before anything scrolls Profile down. Killed two hypotheses first: the login helper (5 of 6 failures use login.yaml, same as all 5 passing flows) and the renamed fixture (only 1 of 6 asserts a name). Added a GUARDED scrollUntilVisible UP. | [Failed] view_seller_profile_from_profile (2m 36s) (Assertion is false: "Ahmad Karimi" is visible) |

## `saved` — Save / unsave a listing, saved tab, sold-while-saved

5/8 passing · 3 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `save_from_browse_feed` | PASS | run-565 | 171 |  |  |
| `save_listing` | FAIL-assert | run-565 | 130 |  | [Failed] save_listing (2m 2s) (No visible element found: "Wool Blanket Handmade King Size") |
| `save_multiple_listings` | PASS | run-565 | 126 |  |  |
| `saved_empty_state` | PASS | run-565 | 135 |  |  |
| `saved_listing_goes_sold` | FAIL-assert | run-565 | 356 |  | [Failed] saved_listing_goes_sold (5m 48s) (Element not found: Id matching regex: seller-card-primary-action) |
| `saved_pagination` | PASS | run-565 | 159 |  |  |
| `unsave_from_browse_feed` | FAIL-assert | run-565 | 139 |  | [Failed] unsave_from_browse_feed (2m 11s) (Assertion is false: "No saved items yet" is visible) |
| `unsave_listing` | PASS | run-565 | 121 |  |  |

## `report` — Report a listing or user, block, block side-effects

5/8 passing · 3 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `block_prevents_message` | PASS | run-563 | 228 |  |  |
| `block_user` | PASS | run-563 | 186 | FIXED run-532 (0fa7390c6d5b/059e9030b846/805ae6b1b316 -> f1c2e80c9580/0dc7a83b3121/0ed779be84ca). The whole report feature went 0/8. Three flows shared one opening — scrollUntilVisible on "Wool Blanket Handmade King Size", centerElement, timeout 15000 — and all three died AT it. Fixture verified over HTTP: feed position 15 of 69, status active; buyer holds ZERO blocks, so the "block_user_hides_listings poisoned the feed" theory is KILLED. THE CONTROL, same run same feature: report_user_then_block opens by SEARCHING and reached the "Unblock User" assertion, i.e. past the opening, while all three scrollers failed at it — same box, same load, only scroll-vs-search differs. Converted to _helpers/open_listing_by_title.yaml. Safe: none of the three asserts anything on the feed CARD. Also O(1) in feed size, which matters because ~14%% of the feed is QA Disposable debris sorted newest-first. AWAITING VERDICT. |  |
| `block_user_hides_listings` | PASS | run-563 | 172 | PASS, but its logcat carries one `Network Error` line — worth watching, not a defect on its own. |  |
| `report_listing` | FAIL-assert | run-563 | 167 | FIXED run-532 (0fa7390c6d5b/059e9030b846/805ae6b1b316 -> f1c2e80c9580/0dc7a83b3121/0ed779be84ca). The whole report feature went 0/8. Three flows shared one opening — scrollUntilVisible on "Wool Blanket Handmade King Size", centerElement, timeout 15000 — and all three died AT it. Fixture verified over HTTP: feed position 15 of 69, status active; buyer holds ZERO blocks, so the "block_user_hides_listings poisoned the feed" theory is KILLED. THE CONTROL, same run same feature: report_user_then_block opens by SEARCHING and reached the "Unblock User" assertion, i.e. past the opening, while all three scrollers failed at it — same box, same load, only scroll-vs-search differs. Converted to _helpers/open_listing_by_title.yaml. Safe: none of the three asserts anything on the feed CARD. Also O(1) in feed size, which matters because ~14%% of the feed is QA Disposable debris sorted newest-first. AWAITING VERDICT. | RIG-004; also gained the duplicate-rule assertion for listings, which nothing covered. **2026-09-14 triage — the fault is NOT `more-options-button`.** The control that settles it: `listing_detail_share` uses the SAME control and is **8/8**, and `open_listing_deep_link` is 10/14. What separates them is HOW THEY REACH THE LISTING: share opens an ARBITRARY feed card (`listing-card` index 0), while every flow in this family opens ONE SPECIFIC seeded fixture by title. That whole family sits at 0-30% (report_user_then_block 0/10, report_listing 1/16, block_user 2/13, report_listing_no_reason 2/11, report_user 3/10, block_prevents_message 4/13, block_user_hides_listings 5/13), so the fragility is REACHING THE FIXTURE, not the kebab. Also ruled out: the overlay's `overlayOpacityAnim` interpolates 1 -> 0.85, never to 0, so the control never becomes an invisible child no matter how far the screen is scrolled. And it is not below the fold — block_user's own recorded hierarchy at a failure held save-toggle-button AND more-options-button at the top. **DO NOT patch the tap, the wait or the scroll.** The direction that is working is reaching the fixture by SEARCH (`_helpers/open_listing_by_title.yaml`); block_user went 1/12 -> 1/1 on it. NEXT: a screenshot from a report_listing failure to confirm which screen it is actually standing on. |
| `report_listing_no_reason` | FAIL-assert | run-563 | 172 | FIXED run-532 (0fa7390c6d5b/059e9030b846/805ae6b1b316 -> f1c2e80c9580/0dc7a83b3121/0ed779be84ca). The whole report feature went 0/8. Three flows shared one opening — scrollUntilVisible on "Wool Blanket Handmade King Size", centerElement, timeout 15000 — and all three died AT it. Fixture verified over HTTP: feed position 15 of 69, status active; buyer holds ZERO blocks, so the "block_user_hides_listings poisoned the feed" theory is KILLED. THE CONTROL, same run same feature: report_user_then_block opens by SEARCHING and reached the "Unblock User" assertion, i.e. past the opening, while all three scrollers failed at it — same box, same load, only scroll-vs-search differs. Converted to _helpers/open_listing_by_title.yaml. Safe: none of the three asserts anything on the feed CARD. Also O(1) in feed size, which matters because ~14%% of the feed is QA Disposable debris sorted newest-first. AWAITING VERDICT. | [Failed] report_listing_no_reason (2m 44s) (Assertion is false: "Please select a reason." is visible) |
| `report_user` | PASS | run-563 | 148 | TRIAGED run-532 — NOT the same cause as the three converted flows. Fails later, on `id: seller-profile-link` on the DETAIL screen, so the listing opened fine. Deliberately left alone; needs its own screenshot. | RIG-004 part 2: retargeted to ahmad (36) so it cannot collide intra-cycle. |
| `report_user_from_profile` | PASS | run-563 | 146 | TRIAGED run-532 — same as report_user: fails on `id: seller-profile-link`, a detail-screen step, not the feed opening. Left alone pending its own screenshot. | Retargeted to omar (37); stopped using nondeterministic listing-card index 0. |
| `report_user_then_block` | FAIL-assert | run-563 | 168 | rig — ran UNAUTHENTICATED. The end-of-flow screenshot's tab bar reads Bazaar / Categories / Login, so `seller-profile-link` (which DOES exist, ListingDetail.tsx:797) was never reachable. Also shows the inputText character drop: the search field holds 'nch 4K Smart TV' — the leading 'Sony 55 i' was dropped. | Retargeted to maryam (40); now unblocks, which it never did. **2026-09-14 triage — the fault is NOT `more-options-button`.** The control that settles it: `listing_detail_share` uses the SAME control and is **8/8**, and `open_listing_deep_link` is 10/14. What separates them is HOW THEY REACH THE LISTING: share opens an ARBITRARY feed card (`listing-card` index 0), while every flow in this family opens ONE SPECIFIC seeded fixture by title. That whole family sits at 0-30% (report_user_then_block 0/10, report_listing 1/16, block_user 2/13, report_listing_no_reason 2/11, report_user 3/10, block_prevents_message 4/13, block_user_hides_listings 5/13), so the fragility is REACHING THE FIXTURE, not the kebab. Also ruled out: the overlay's `overlayOpacityAnim` interpolates 1 -> 0.85, never to 0, so the control never becomes an invisible child no matter how far the screen is scrolled. And it is not below the fold — block_user's own recorded hierarchy at a failure held save-toggle-button AND more-options-button at the top. **DO NOT patch the tap, the wait or the scroll.** The direction that is working is reaching the fixture by SEARCH (`_helpers/open_listing_by_title.yaml`); block_user went 1/12 -> 1/1 on it. NEXT: a screenshot from a report_listing failure to confirm which screen it is actually standing on. |

## `reviews` — Double-blind reviews after a sold transaction

1/3 passing · 2 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `pending_reviews_nudge` | FAIL-assert | run-564 | 228 |  | [Failed] pending_reviews_nudge (3m 40s) (Element not found: Text matching regex: Submit review) |
| `profile_reviews_empty_state` | PASS | run-564 | 135 |  |  |
| `rate_buyer_after_sale` | FAIL-assert | run-564 | 209 |  | [Failed] rate_buyer_after_sale (3m 21s) (Assertion is false: id: seller-listing-card is visible) |

## `auth` — Sign up, login, logout, session persistence, guest gating

12/16 passing · 1 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `confirm_email_prompt` | PASS | run-567 | 158 |  |  |
| `guest_browse` | PASS | run-567 | 131 |  |  |
| `guest_offer_redirect` | PASS | run-567 | 194 |  |  |
| `guest_save_redirect` | FAIL-assert | run-567 | 200 |  | [Failed] guest_save_redirect (3m 12s) (Assertion is false: "Wool Blanket Handmade King Size" is visible) |
| `login` | PASS | run-567 | 144 |  |  |
| `login_deep` | PASS | run-567 | 195 |  |  |
| `login_empty_fields` | PASS | run-567 | 118 |  | Request failed with status code Request failed with status code |
| `login_navigate_to_register` | PASS | run-567 | 118 |  |  |
| `login_wrong_password` | PASS | run-567 | 126 |  | Request failed with status code |
| `logout` | PASS | run-567 | 187 | rig | ENVIRONMENT, not the flow. run-241 aborted mid-feature: an openaleph-mobile Gradle build took the load average to 49 on 16 cores and this session's emulator died — the rig logged "CPU only 0% idle — refusing to boot" and "could not recover the emulator — aborting feature 'auth'". Re-run on a quiet machine before reading anything into it. logout is also the reference flow that showed sign-out lands on the Bazaar (see login_deep). |
| `logout_cancel` | PASS | run-567 | 182 |  |  |
| `register_duplicate_email` | FAIL-assert ⟳stale | run-567 | 144 | flow | APP IS CORRECT (422 + errors.full_messages surfaced) but the FLOW was wrong, and my first diagnosis blamed the wrong thing. Register.tsx renders each error as `<Text>{"• "}{msg}</Text>`, so the node reads "• Email has already been taken" and Maestro's anchored regex cannot match the bare literal. It would have failed on a quiet machine too — the `Refreshing…` banner in the first screenshot was real but incidental. Now asserts ".*Email has already been taken.*". |
| `register_navigate_to_login` | PASS | run-567 | 118 |  |  |
| `session_persist` | PASS | run-567 | 148 |  |  |
| `sign_up` | FAIL-assert ⟳stale | run-567 | 145 |  | [Failed] sign_up (2m 17s) (Element not found: Id matching regex: register-email-input) |
| `sign_up_validation` | FAIL-assert ⟳stale | run-567 | 177 |  | [Failed] sign_up_validation (2m 49s) (Element not found: Text matching regex: Buy and sell locally.*) |

## `maps` — Location pickers — create-listing pin, Browse filter range, current location, permissions

6/7 passing · 1 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `create_listing_map_pin` | PASS | run-568 | 198 | session-isolation | s2/run-550: 2 of 4 passed at this same sha, and the failures do not time out — they report the app sitting LOGGED OUT (`"Login" is not visible` / `Post a listing` missing). That is the identity-switch root cause already named for `listing_actions_sheet`, not a map defect. Fix is session isolation; do not patch this flow. |
| `filter_map_default_kabul` | PASS | run-568 | 163 | contention | s2/run-550 ran this flow FOUR times at ONE sha; the passes are 196-276s and every fail is a long run (388-415s) under booklet's `parallel_rspec -n 8` (load 33). Same file, same run, same session — elapsed time is the only variable, so this is host contention, not a regression. Not a verdict; re-run when load is under ~10. |
| `filter_map_location_denied` | PASS | run-568 | 179 | contention | s2/run-550 ran this flow FOUR times at ONE sha; the passes are 196-276s and every fail is a long run (388-415s) under booklet's `parallel_rspec -n 8` (load 33). Same file, same run, same session — elapsed time is the only variable, so this is host contention, not a regression. Not a verdict; re-run when load is under ~10. |
| `filter_map_use_my_location` | PASS | run-568 | 170 | contention | s2/run-550 ran this flow FOUR times at ONE sha; the passes are 196-276s and every fail is a long run (388-415s) under booklet's `parallel_rspec -n 8` (load 33). Same file, same run, same session — elapsed time is the only variable, so this is host contention, not a regression. Not a verdict; re-run when load is under ~10. |
| `filter_map_use_my_location_granted` | PASS | run-568 | 162 |  |  |
| `map_location_outside_afghanistan` | PASS | run-568 | 190 | session-isolation | s2/run-550: 2 of 4 passed at this same sha, and the failures do not time out — they report the app sitting LOGGED OUT (`"Login" is not visible` / `Post a listing` missing). That is the identity-switch root cause already named for `listing_actions_sheet`, not a map defect. Fix is session isolation; do not patch this flow. |
| `zoom_controls_not_occluded` | FAIL-assert | run-568 | 147 |  | [Failed] zoom_controls_not_occluded (2m 18s) (No visible element found: "Toyota Corolla 2016 Automatic") |

## `gallery` — Listing photo upload, carousel, reorder, empty-photo state

3/4 passing · 1 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `listing_create_multi_photos` | FAIL-assert | run-562 | 254 | flow | Asserted the reorder hint with nothing selected; hint needs selectedIdx !== -1. |
| `listing_edit_add_photos` | PASS | run-562 | 157 | flow | 2026-09-02: never scrolled to its own Save button, which adding a photo pushes below the fold — the rule this file's own header states. Now scrolls at visibilityPercentage 40. |
| `listing_gallery_no_photo` | PASS | run-562 | 154 |  |  |
| `listing_gallery_swipe` | PASS | run-562 | 124 |  |  |

## `mode` — Buyer ↔ seller mode switch, tab bar, persistence

3/4 passing · 1 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `seller_mode_my_listings_empty` | FAIL-assert | run-566 | 156 | candidate — one spurious 401 logged the app out; see UI_FINDINGS, needs 2nd sighting | new_seller@hatiwal.test was referenced by the flow and seeded nowhere. |
| `seller_mode_persists` | PASS | run-566 | 197 |  |  |
| `seller_mode_tab_bar_changes` | PASS | run-566 | 128 |  |  |
| `seller_views_own_listing_buyer_mode` | PASS | run-566 | 186 | flow | Searched the feed for "seller"; search matches titles, so it found nothing. |

## `safety` — Safety tips on listing detail and in the meetup sheet

1/2 passing · 1 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `safety_tips_listing_detail` | PASS | run-569 | 181 |  |  |
| `safety_tips_meetup_sheet` | FAIL-assert | run-569 | 157 |  | [Failed] safety_tips_meetup_sheet (2m 29s) (Element not found: Text matching regex: Safety tips) |

## `rtl` — Pashto + Dari right-to-left layout across main screens

8/10 passing · 1 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `browse_rtl_dari` | PASS | run-557 | 206 |  |  |
| `browse_rtl_pashto` | PASS | run-557 | 480 |  |  |
| `buyer_picker_rtl` | FAIL-assert | run-557 | 533 |  | [Failed] buyer_picker_rtl (8m 41s) (Assertion is false: "Electric Kettle 1.8L Stainless - Bulk 10" is visible) |
| `categories_hub_rtl` | PASS | run-557 | 194 | flow | 2026-09-02: tapped text "Back" in a flow whose whole purpose is Pashto. The app renders no literal "Back" — BackButton has accessibilityLabel t(common.goBack) (ps شاته ځه) and testID back_button. Now targets the testID. |
| `chat_rtl` | FAIL | run-557 | 498 | MOSTLY RIG, NOT 0/33. The headline count is misleading: of 33 recorded failures at sha 22f997ea3df9, ~22 are kind=rig_devclient_crash — the documented Expo dev-menu FAB crash (QA_HANDBOOK). classify() and tally.py already exclude those; a naive count that does not filter `kind` inflates this flow into the suite's worst offender, which is how it reached the top of the triage list. run-557, the newest, is one of them. Only THREE runs (s2/522, s2/530, s2/531) lack the FAB signature and are genuinely measured. | [Failed] chat_rtl (8m 8s) (Element not found: Text matching regex: لیږل) |
| `listing_detail_rtl` | PASS | run-557 | 513 |  | AxiosError **2026-09-13 21:05 — THIS RESULT IS VOID, NOT A VERDICT.** It ran against a broken Metro bundle: adding `src/i18n/locales/ur/` created a NEW DIRECTORY, which Metro does not register until restarted, so every flow on both emulators red-screened with UnableToResolveError. Fixed by `docker restart hatiwal-mobile-mobile-1`. Do not triage this run — re-judge on the next clean pass. See QA_HANDBOOK.md. |
| `my_listings_rtl` | PASS | run-557 | 234 |  | **2026-09-13 21:05 — THIS RESULT IS VOID, NOT A VERDICT.** It ran against a broken Metro bundle: adding `src/i18n/locales/ur/` created a NEW DIRECTORY, which Metro does not register until restarted, so every flow on both emulators red-screened with UnableToResolveError. Fixed by `docker restart hatiwal-mobile-mobile-1`. Do not triage this run — re-judge on the next clean pass. See QA_HANDBOOK.md. |
| `profile_quick_actions_rtl` | PASS | run-557 | 274 | flow | 2026-09-02 SOLVED, flow bug, b20007e: the language-revert hypothesis was WRONG, and so was the mode-toggle one. Profile.tsx:306 renders this row as `${t('…myListings')} (${count})`, so the text is 'زما اعلانونه (0)' and Maestro's anchored regex could not match the bare label — the three sibling labels carry no suffix, which is why only this row failed. run-379's screenshot shows Profile correctly in seller mode (green tab bar, three tabs, saved-tab gone). Matched as a prefix now. NOT an app bug. **2026-09-13 21:05 — THIS RESULT IS VOID, NOT A VERDICT.** It ran against a broken Metro bundle: adding `src/i18n/locales/ur/` created a NEW DIRECTORY, which Metro does not register until restarted, so every flow on both emulators red-screened with UnableToResolveError. Fixed by `docker restart hatiwal-mobile-mobile-1`. Do not triage this run — re-judge on the next clean pass. See QA_HANDBOOK.md. |
| `profile_rtl` | PASS | run-557 | 201 | flow? | 2026-09-02: expects fa profile.editProfile "ویرایش پروفایل", which EXISTS verbatim in the locale file — so not a stale selector. Hypothesis: the language-revert bug (fixed 8097ab3) left the app in English after the switch, so no translated string could match. Re-running on a build with that fix. |
| `sales_ledger_rtl` | PASS | run-557 | 272 |  |  |

## `newfeatures` — Pakistan expansion — category search, PKR, Urdu (the CURRENT work)

6/6 passing · 0 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `_open_category_picker` | PASS | s2/run-550 | 311 |  |  |
| `category_search_clear_restores` | PASS | s2/run-550 | 254 |  |  |
| `category_search_empty_state` | PASS | s2/run-550 | 256 |  |  |
| `category_search_finds_subcategory` | PASS | s2/run-550 | 270 |  |  |
| `category_search_parent_still_drills` | PASS | s2/run-550 | 254 |  |  |
| `category_search_urdu_name` | PASS | s2/run-550 | 282 |  |  |

## `onboarding` — First-run experience

1/1 passing · 0 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `first_run` | PASS | run-572 | 236 |  |  |

## `share` — Deep links into a listing and a seller profile

2/2 passing · 0 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `open_listing_deep_link` | PASS | run-570 | 60 |  |  |
| `open_seller_deep_link` | PASS | run-570 | 57 | FIXED run-536 (sha ca0d7f040e31, 0/9 -> new sha c1457c1f1b18). It asserted the WRONG SCREEN'S SELECTOR: `more-options-button` is defined ONLY in src/screens/shared/ListingDetail.tsx:1003, the LISTING detail screen, while this flow opens hatiwal://seller/<id> which renders UserProfile — a different screen that has never carried that testID. It could never pass. IT LOOKED LIKE A REGRESSION AND WAS NOT: 2/2 at older shas, 0/9 now. But 8cccda3's own commit message says this flow "was a test that could not fail", so those two passes were worthless; tightening it (8cccda3, then 0d7b45a for the hardcoded env ids) exposed a selector that had been wrong all along. A flow going from pass to fail after being MADE MEANINGFUL is not a regression. FIXTURE VERIFIED, NOT ASSUMED: the rig injects SELLER_ID=447 (it was 432 earlier — ids move on every re-seed, and 432 now 404s), and 447 really is "Omar Noori", the name this flow hardcodes. So the name assert was fine; only the id was wrong. Replaced with UserProfile.tsx:377 testID="more-menu", the Block/Report overflow, which renders when `!isMe` — a GUEST viewing someone else's profile, exactly this flow's case. AWAITING VERDICT. |  |

## `pagination` — Infinite scroll across browse, search, saved, chat, my-listings

6/6 passing · 0 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `browse_pagination` | PASS | run-571 | 170 |  |  |
| `conversations_pagination` | PASS | run-571 | 124 |  | AxiosError |
| `filter_combined_pagination` | PASS | run-571 | 130 | flow — assertNotVisible on dead copy (vacuous); now asserts a cross-category listing is absent |  |
| `my_listings_pagination` | PASS | run-571 | 173 |  |  |
| `saved_pagination_deep` | PASS | run-571 | 164 |  |  |
| `search_pagination` | PASS | run-571 | 178 |  |  |

## `dark_mode` — Every main screen in dark theme + theme persistence

8/8 passing · 0 open

| Flow | Status | Last run | Secs | Triage | Notes |
|---|---|---|---:|---|---|
| `browse_dark` | PASS | run-558 | 207 |  |  |
| `chat_dark` | PASS | run-558 | 175 |  |  |
| `listing_detail_dark` | PASS | run-558 | 170 |  |  |
| `my_listings_dark` | PASS | run-558 | 200 | MY REGRESSION — restart helper waited for listing-card; seller mode returns to seller-listing-card. Fixed |  |
| `profile_dark` | PASS | run-558 | 179 | flow — same toothless restart wait; fixed cb68fa4 | UI-048 OPEN: ended on the Bazaar feed mid-flow, cause not established. Checkpointed. |
| `saved_tab_dark` | PASS | run-558 | 164 |  |  |
| `theme_light_all_screens` | PASS | run-558 | 186 |  |  |
| `theme_persists_after_navigate` | PASS | run-558 | 217 | flow — same toothless restart wait; fixed cb68fa4 | UI-048 OPEN: same. Waited on profile-tab, which is visible on every tab. |
