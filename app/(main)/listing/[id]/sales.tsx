// SF-M5 (docs/SELL_FLOW_REDESIGN.md §10.3) — reads the same [id] param as the
// parent /(main)/listing/[id] detail route. Reached from
// useListingLifecycle's "View sales" moreActions row.
import ListingSales from "@/screens/seller/ListingSales";

export default ListingSales;
