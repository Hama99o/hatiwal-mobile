// REV2 — dedicated "View all reviews" screen for a user. Reads the same
// [id] param as the parent /(main)/user/[id] profile route.
import AllReviewsScreen from "@/screens/shared/AllReviews";
export default AllReviewsScreen;
