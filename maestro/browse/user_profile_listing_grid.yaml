appId: com.hatiwal.app
---
# Seller profile from browse: listings grid shows seller's active listings

- runFlow: ../_helpers/login.yaml
- tapOn: "Bazaar"
- waitForAnimationToEnd
# A bare `tapOn: index:` has NO selector: it matches every element on screen,
# so index 0 is whatever lands first in hierarchy order — never the feed card.
# The flow stayed on the feed and failed further down on the detail screen it
# never opened.
- tapOn:
    id: "listing-card"
    index: 0
- waitForAnimationToEnd
- assertVisible: "Message Seller"

# Tap seller name/avatar → seller profile
- tapOn:
    id: "seller-name"
- waitForAnimationToEnd
- assertVisible: "Seller Profile"

# Listings grid visible
- assertVisible: "Listings"
- assertVisible:
    id: "listing-card-0"

# Tap a listing in the grid → goes to listing detail
- tapOn:
    id: "listing-card-0"
- waitForAnimationToEnd
- assertVisible: "Message Seller"

- back
- waitForAnimationToEnd
- assertVisible: "Seller Profile"
