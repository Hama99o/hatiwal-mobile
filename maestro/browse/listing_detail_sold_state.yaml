appId: com.hatiwal.app
---
# Sold listing detail — shows sold notice, no contact button

- runFlow: ../_helpers/login.yaml
- assertVisible: "Bazaar"
- waitForAnimationToEnd
# The seller@hatiwal.test account has a "sold" listing in the feed
# Search for it or scroll until found
- assertVisible: "Bazaar"
# Scroll to find a listing with "Sold" overlay
- scroll
- scroll
- waitForAnimationToEnd
# Open a sold listing detail (if the sold one appears in feed)
# Note: sold listings may be filtered out of browse — this tests the edge case
# where a user navigates directly to a sold listing URL via deep link
# This flow verifies the "sold notice" banner if shown
- assertVisible: "Bazaar"
