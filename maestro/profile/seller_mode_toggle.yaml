appId: com.hatiwal.app
---
# Profile screen — switch to seller mode from the profile tab

- runFlow: ../_helpers/login.yaml
- tapOn: "Me"
- waitForAnimationToEnd
# NOT "Profile": `profile.title` is a dead key, rendered nowhere — the screen
# shows the user's NAME. Anchor on the mode toggle's label instead: it is
# high on the screen (just under the avatar), needs no scroll, and the regex
# keeps it independent of which mode the account is in.
#
# Not "Edit" either, though that IS a real control here: it lives in
# PersonalInfoCard, well below the fold, so asserting it without a scroll
# just trades one guaranteed failure for another.
- assertVisible: "Switch to .*"
# Tap "Switch to Seller Mode" option in profile
- tapOn: "Switch to Seller Mode"
- waitForAnimationToEnd
# Mode changed — banner confirms seller mode
# NOT "Seller Mode" (`profile.sellerMode`, dead with the banner). The toggle now
# offering BUYER mode is what proves the switch happened.
- extendedWaitUntil:
    visible: "Switch to Buyer Mode"
    timeout: 20000
# Profile tab now shows seller-focused content
- tapOn: "Me"
- waitForAnimationToEnd
# Switch back
- tapOn: "Switch to Buyer Mode"
- waitForAnimationToEnd
# NOT "Buyer Mode" (dead with the banner). Back in buyer mode, the toggle offers
# SELLER mode again.
- extendedWaitUntil:
    visible: "Switch to Seller Mode"
    timeout: 20000
