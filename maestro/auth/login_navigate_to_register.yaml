appId: com.hatiwal.app
---
# From login screen: tap "Create account" → lands on register screen

- launchApp:
    clearState: true
- runFlow: ../_helpers/open_bundle.yaml
- runFlow: ../_helpers/goto_login.yaml
- waitForAnimationToEnd
# NOT "Welcome to Hatiwal": the onboarding carousel's FIRST SLIDE and the login
# screen both render that exact heading (onboarding.slides.welcome.title and
# auth.welcome), so asserting it cannot tell the two apart — a flow could pass
# this line while sitting on onboarding and then fail on the next one.
# auth.subtitle appears on the login screen only.
#
# extendedWaitUntil, not assertVisible: after `clearState` the dev client
# re-fetches the JS bundle, and a bare assert can fire before the first screen
# has rendered at all. guest_browse failed exactly that way after passing minutes
# earlier — neither screen was up yet.
- extendedWaitUntil:
    visible: "Buy and sell locally in Afghanistan"
    timeout: 60000
- tapOn: "Sign In"
- waitForAnimationToEnd
- assertVisible: "Sign In"
# The login screen renders this as ONE Text — "Don't have an account? Create
# Account" — so no exact-match selector on either casing can find it. (This flow
# used a lowercase "a", which is why the earlier sweep for the exact string
# missed it.)
- tapOn:
    id: "go-to-register"
- waitForAnimationToEnd
- assertVisible: "Create Account"
# Register.tsx renders `{t("auth.haveAccount")} {t("auth.loginButton")}` as ONE
# text node — "Already have an account? Sign In" — and Maestro matches the whole
# string, so asserting the first half alone never matched. Unlike "Sign In" and
# "Create Account", which are also standalone button labels, this half exists
# nowhere else in the app.
# No backslash: `\?` is not a valid escape inside a YAML double-quoted string, and
# it is not needed — as a regex, `account?.*` matches "accoun" + an optional "t",
# then `.*` absorbs "? Sign In".
- assertVisible: "Already have an account?.*"
