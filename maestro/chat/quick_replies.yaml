appId: com.hatiwal.app
---
# TASK-Q374 — Quick-reply chips in the chat composer
# Flow: open a conversation → tap a quick-reply chip → assert text is in the
#        composer → edit it → tap Send → assert the message bubble appears.

- runFlow: ../_helpers/login.yaml
# The Chats TAB by testID. "Chats" is also `listing.ownerDetail.conversations`,
# a label on the seller's owner-detail screen, so a text tap can land on that
# instead — Maestro takes the first match, and the flow then carries on
# believing it navigated. The tab id is unambiguous and locale-independent.
- tapOn:
    id: "chat-tab"
- waitForAnimationToEnd

# Open the first available conversation
# A bare `tapOn: index:` has NO selector, so it matches EVERY element on screen
# and index 0 is whatever comes first in the hierarchy — the heading or the
# search bar, never a conversation. The flow stayed on the list and then failed
# on the composer it never reached ("Type a message..." is visible).
- tapOn:
    id: "conversation-row-\\d+"
    index: 0
- waitForAnimationToEnd

# The quick-reply chip row should be visible (conversation is open)
# Tap the first buyer chip: "Is this still available?"
- tapOn: "Is this still available?"
- waitForAnimationToEnd

# The phrase should now be in the composer input
- assertVisible: "Is this still available?"

# The user can edit the draft — add extra text
# DO NOT RE-TAP THE TEXT TO FOCUS IT. This step used to be
# `- tapOn: "Is this still available?"` a second time, and it CORRUPTED the draft.
#
# Maestro taps an element's CENTRE. Tapping the middle of a field that already
# contains text puts the CARET THERE, and the inputText below then inserts at that
# caret instead of appending. run-575 sent this, verbatim:
#
#     Is this still availa Please let me know.ble?
#
# — "Is this still available?" split after "availa", with the new text spliced in.
# The flow then failed asserting the sentence it meant to send. Not an app bug: a
# tap in the middle of a text field is SUPPOSED to move the caret there.
#
# The re-tap was also unnecessary. handleQuickReplySelect (Conversation.tsx:1434)
# appends the phrase to the draft and then focuses the input itself — its own
# comment says "cursor lands at the end" — so the field is already focused with the
# caret in the right place when the chip tap returns. Typing straight after it
# appends, which is what this flow wants.
#
# Note the assert above is satisfied by BOTH the chip and the composer's contents,
# so it cannot tell you which one a tap would hit. That ambiguity is exactly what
# made the re-tap look safe.
- inputText: " Please let me know."
- waitForAnimationToEnd

# Tap the existing Send button — identified by its accessibility label (the button
# renders only a lucide Send icon with no visible text, so we match by label)
- tapOn:
    text: "Send"
- waitForAnimationToEnd

# The message bubble should appear in the thread
- assertVisible: "Is this still available? Please let me know."

# ── Error / empty state ───────────────────────────────────────────────────────
# (If no conversations exist the screen shows an empty state — already covered
# by conversations_empty_state.yaml. This flow requires at least one thread.)
