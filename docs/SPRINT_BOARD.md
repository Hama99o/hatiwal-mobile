# Hatiwal Sprint Board

> **Machine-readable task board** for the continuous software-house loop.
> Owned by the orchestration workflow — do not manually edit IN_PROGRESS tasks.
>
> **Statuses:** `AVAILABLE` · `IN_PROGRESS` · `CHANGES_REQUESTED` · `DONE` · `STUCK`
> **Types:** `frontend` · `backend` · `fullstack` · `design` · `product`
> **Priorities:** `P0` (critical/blocking) · `P1` (core MVP) · `P2` (polish)

---
## TASK-D002
- **Title**: Build conversation thread screen (gifted-chat, meetup proposal)
- **Type**: fullstack
- **Priority**: P1
- **Status**: CHANGES_REQUESTED
- **Session**: -
- **Blocks**: -
- **BlockedBy**: -
- **ReviewNotes**: CRITICAL — Block toggle / participant header never render. File: /home/hama99o/Apps/Personal/Hatiwal/hatiwal-mobile/src/screens/chat/Conversation.tsx:437 (`const otherParticipant = conversation?.otherParticipant`). The screen loads the conversation via conversationsAPI.getConversation → GET /conversations/:id, which uses ConversationSerializer view :detailed. That view (app/serializers/conversation_serializer.rb:44-58) returns `buyer`, `seller`, `blocked_with_participant` — it does NOT return `other_participant` (only the :list view does, line 8). So `conversation.otherParticipant` is always undefined on the thread screen, which makes the block/unblock button (`{otherParticipant && (...)}` line 557), the nav title showing the participant name (line 529), and the tap-to-seller-profile link (line 523) all silently disappear. The block/unblock feature claimed in the summary is non-functional. FIX: add an `other_participant` field to the :detailed serializer view (mirroring the :list view, using opts[:current_user] which the show action already passes), OR derive otherParticipant on the client from buyer/seller + currentUser.id. Add a request-spec assertion that the detailed response includes the participant the screen relies on. | CRITICAL — Backend suite is not deterministically green. File: /home/hama99o/Apps/Personal/Hatiwal/hatiwal-api/spec/requests/api/v1/messages_spec.rb:403 (mark_read 'exactly one UPDATE … no N+1'). Confirmed FLAKY: ran the example 3 times, it failed once ('Expected exactly 1 UPDATE for 15 unread messages but got 2'). The extra UPDATE is an intermittent devise-token-auth token-rotation UPDATE on the user firing during the second/third PUT in the test window. This violates the mandatory pre-commit rule (bundle exec rspec must pass with zero failures) and will cause random CI failures. The developer summary's '71 specs, 0 failures' is false on a fraction of runs. FIX: scope the UPDATE counter to the messages table (e.g. count only `UPDATE \"messages\"`), or freeze time / disable token rotation in the test, so the assertion isn't polluted by auth-token writes. The production code (update_all) is correct — the test is the defect. | MINOR — Non-localized currency formatting. File: /home/hama99o/Apps/Personal/Hatiwal/hatiwal-mobile/src/screens/chat/conversation/MessageBubble.tsx:175-176. The offer bubble builds `${currency} ${amount.toLocaleString()}` using raw JS toLocaleString instead of useLocalization().formatCurrency/formatNumber. mobile.prompt.md §4 makes localized numbers/currency mandatory (must use useLocalization, never raw formatting). FIX: use formatCurrency(amount, currency) from useLocalization() for the offer amount and listed price. | GOOD — Translations are complete: all 92 nested chat.json keys match across en/ps/fa with zero missing/extra; every key the screen references (thread, meetup, offer, block, search, startConversation, document) exists in all three locales. | GOOD — Backend RuboCop clean on all 8 changed Ruby files; ConversationPolicy enforces participant?/open/blocked-pair correctly; MessagesController kind whitelist coerces unknown/:system kinds to :system so the model validation returns 422; responds_to_must_be_in_same_conversation guards cross-conversation outcome corruption; index/show use policy_scope; render_blue/paginate_blue used (no render json:). | NOTE — `isBlocked` state (Conversation.tsx:127) initializes to false and is never seeded from conversation.blockedWithParticipant. Even after the otherParticipant bug above is fixed, the ShieldBan icon will always show the 'not blocked' state on initial load until the user toggles. FIX: initialize/sync isBlocked from conversation.blockedWithParticipant in the load() effect. | LIBRARY COMPLIANCE (major) — src/screens/chat/Conversation.tsx: The chat thread is hand-rolled with a raw FlatList + custom MessageBubble. Both BACKLOG D2 ('react-native-gifted-chat themed to tokens, RTL bubbles, read receipts') and DESIGN_SYSTEM.md §4 ('Chat thread UI → react-native-gifted-chat … Don't build a chat list') mandate gifted-chat. The package is NOT installed (confirmed: node_modules/react-native-gifted-chat absent). Fix: either (a) migrate the message list to react-native-gifted-chat with a reanimated renderMessage wrapper as the spec describes, or (b) record an explicit, dated waiver in docs/BACKLOG.md D2 justifying the bespoke list (e.g. RTL/offer-card needs gifted-chat can't meet) so future reviewers don't flag it again. Right now the code silently contradicts the spec. | LIBRARY COMPLIANCE (major) — src/screens/chat/conversation/MeetupSheet.tsx: Uses a raw React Native <Modal animationType='slide'> with a hand-built backdrop + drag handle. DESIGN_SYSTEM.md §4 names @gorhom/bottom-sheet as THE standard for 'category, report reason, filters, meetup' and §9 forbids a 'hand-rolled … sheet'. BACKLOG D2 also specifies the meetup action 'via @gorhom/bottom-sheet (place/time)'. The file comment cites 'mobile.prompt.md §5 slide-up pattern' as justification — if that prompt genuinely sanctions a raw Modal for this case, the two docs conflict and the conflict must be resolved in the docs; otherwise convert MeetupSheet to @gorhom/bottom-sheet. As-is it diverges from the named library for this exact use case. | TOUCH TARGETS (minor) — src/screens/chat/conversation/MessageBubble.tsx lines 306-323 (offer Accept/Decline) and 440-457 (meetup Accept/Decline): buttons use paddingVertical:9 with ~13px text, yielding roughly 38px tall — under the 44px minimum from DESIGN_SYSTEM.md §3. These are primary deal-advancing actions ('Move the deal forward'), so they should be the comfortable to hit. Fix: bump to paddingVertical:12 (or minHeight:44) on all four response buttons. | TOUCH TARGETS (minor) — src/screens/chat/Conversation.tsx: nav actions (styles.navAction padding:4, ~26px) and the search Input (height:36, line 905) sit below 44px. hitSlop={8} on the icon Pressables mitigates the icons but the search Input itself has no hitSlop and is a 36px tap target. Fix: raise searchInput minHeight to 44 (visually it can stay compact via vertical padding) and confirm nav icon tap area reaches 44 with hitSlop. | TOKENS (minor) — src/screens/chat/conversation/MessageBubble.tsx lines 152-153 and 265: metaColor/readColor use literal 'rgba(255,255,255,0.65)'/'0.9' and the offer divider uses template-string `${colors.warning}40`. These are deliberate translucent-white-on-primary and alpha-on-warning values, but DESIGN_SYSTEM.md §2/§9 says 'no hex' and 'All colors via useColors()'. useColors already exposes alpha tokens (primaryAlpha, warningAlpha, successAlpha, darkScrim). Fix: add a primaryForegroundMuted / onPrimaryMeta token (and reuse an existing alpha for the divider) so even these meta colors flow through useColors and stay dark-mode-correct. | POSITIVE — RTL is handled thoroughly and correctly across all three files (row-reverse, textAlign flips, mirrored Send icon via scaleX:-1, ChevronLeft/Right swap, mine-bubble anchor flip via isMine !== isRtl). Dark mode is correct: useColors() is used everywhere for surfaces/borders/text. States are complete: ChatSkeleton mirrors layout (not a bare spinner), empty-thread shows listing header + EmptyState copy, send failures roll back optimistically with a sonner toast. Reduce-motion is respected in both the search animation and bubble entrances. Read receipts, offer/meetup outcome badges, and the pinned ListingHeader (RemoteImage=expo-image + PriceTag + StatusBadge + location) all build trust and keep the item context visible. This is a strong, polished screen — only the library-mandate reconciliation and the small target/token nits stand between it and APPROVED. | NOTE — ListingHeader thumbnail is 40x40 (not ≥200px). This is CORRECT and not a finding: the design north star's 'hero photo ≥200px' applies to listing detail/cards, whereas a pinned chat header should be compact. Price uses PriceTag size='sm' which is appropriate for this dense context.
- **Description**: Backend: ensure GET /conversations/:id (detailed: listing, buyer, seller), GET /conversations/:id/messages (paginated asc), POST /conversations/:id/messages (body, kind), POST /listings/:listing_id/conversations (start flow). Mobile: src/screens/chat/Conversation.tsx using react-native-gifted-chat themed to NativeWind tokens. Pinned listing header card (thumbnail + PriceTag + StatusBadge). RTL bubbles. Read receipts (read_at). Meetup proposal action (kind: meetup_proposal) via @gorhom/bottom-sheet (place+time) → special bubble. Start flow: first-message sheet from listing detail; 422 (inactive/self/duplicate) → friendly toast → open existing if duplicate. Closed conversation → input disabled with notice. States: loading skeleton, empty thread (just listing header), send failure toast. Route: app/(main)/conversation/[id].tsx
- **Acceptance**: Can start from a listing and exchange messages; RTL bubbles correct; pinned listing visible; meetup proposal works
## TASK-P401
- **Title**: Micro-interactions: input focus, empty state illustrations, toast polish
- **Type**: frontend
- **Priority**: P1
- **Status**: AVAILABLE
- **Session**: -
- **Blocks**: -
- **BlockedBy**: TASK-P501
- **ReviewNotes**: -
- **Description**: ## Goal P4 — Micro-interactions. Small targeted interactions that make the app feel polished and alive.  ## Dependency P1 animation system (AnimatedPressable, haptics.ts) must exist. Check P1 card before starting.  ## Sub-features  ### 1. Form input focus animations - When an Input or Textarea gains focus: border color transitions from border to primary over 150ms (withTiming) - Label text transit
- **Acceptance**: Ship with no console errors. All mobile coding rules satisfied.
## TASK-Q501
- **Title**: End-to-end manual test on iOS simulator and Android emulator
- **Type**: frontend
- **Priority**: P0
- **Status**: CHANGES_REQUESTED
- **Session**: -
- **Blocks**: -
- **BlockedBy**: TASK-Q301
- **ReviewNotes**: FALSE TEST CLAIM (must fix): `npx jest --watchAll=false` reports `Tests: 1 failed, 584 passed, 585 total` — NOT 585/585 as the summary states. The failure is in /home/hama99o/Apps/Personal/Hatiwal/hatiwal-mobile/src/components/common/__tests__/SavedSearches.test.tsx, test 'SavedSearches — optimistic delete › calls savedSearchesAPI.delete with the correct id when X is tapped'. It fails at line 267 …
- **Description**: ## Goal Q5 — Testing on Real Devices. Final gate before a production build.  ## Dependency Q1, Q2, Q3, Q4 must all be Done before this starts.  ## Test environment - iOS: physical iPhone or Xcode simulator (iOS 17+) - Android: physical device or Android emulator (API 34+) - Run the app via: npx expo run:ios and npx expo run:android  ## Full checklist (run on BOTH platforms) 1. App launches from cold start...
- **Acceptance**: Ship with no console errors. All mobile coding rules satisfied.
## TASK-N804
- **Title**: Listing price history: track price changes and show a badge on detail
- **Type**: fullstack
- **Priority**: P2
- **Status**: CHANGES_REQUESTED
- **Session**: -
- **Blocks**: -
- **BlockedBy**: -
- **ReviewNotes**: CORRECTNESS (E2E false-positive) — maestro/browse/listing_detail_price_drop_badge.yaml and maestro/listings/price_drop_after_edit.yaml: the assertions that verify the badge (assertVisible text \"-%\" and \"% price drop\") are all marked `optional: true`. An optional assertion never fails the flow, so these E2E tests pass even when the badge is completely missing — they provide zero real coverage of the feature they exist to test. Fix: make the core badge assertions non-optional and seed deterministic data (a listing with a ListingPriceHistory created <14 days ago, new_price<old_price) so the badge is guaranteed present. | CORRECTNESS (wrong matcher) — same two Maestro files: the card badge renders `\"-15%\"` (key listing.priceDrop.badgeCardShort => `-{{percent}}%`) and the detail badge renders `\"15% price drop\"`. The matchers `text: \"-%\"` and `text: \"% price drop\"` will not substring/regex-match those strings (e.g. `-%` does not occur in `-15%` because the digits sit between `-` and `%`). Fix: use a regex like `text: \"-\\\\d+%\"` for the card overlay and `text: \"\\\\d+% price drop\"` for the detail variant. | CORRECTNESS (broken flow premise) — maestro/listings/price_drop_after_edit.yaml lines 18-30: it opens an Active listing's edit form and taps \"Save Draft\", which moves the listing to draft/unpublished status. After `back` it expects the listing under the \"Active\" tab, but a drafted listing no longer appears there, and the price-drop badge target card is gone. Fix: tap the real Save/publish action (the edit form's primary save) instead of \"Save Draft\", or assert against the Draft filter. | MINOR (doc drift, not a bug) — src/api/listings.ts lines 63-65: the comment says priceDroppedAt/priceDropPercent are \"only present on :detailed view\", but the serializer also exposes them on :list and :seller_list (and ListingCard correctly reads listing.priceDropPercent for cards). Update the comment to avoid misleading future readers. | NON-BLOCKING OBSERVATION (pre-existing) — app/controllers/api/v1/listings_controller.rb:29 adds `.includes(:price_histories)` (correct for this feature) but the :list serializer view also reads l.user and l.category per row without eager loading them. That seller/category N+1 predates this task (the index never included :user/:category), so it is out of scope here, but worth a follow-up card to add `.includes(:user, :category, :price_histories)`. | VERIFIED PASSING — backend: `bundle exec rspec` on the two new spec files => 29 examples, 0 failures; `bundle exec rubocop` on all 7 changed Ruby files => no offenses. Mobile: `jest PriceDropBadge.test.tsx` => 8 passed. Translations complete and correct in en/ps/fa (listing.priceDrop.badge + badgeCardShort). No rule violations found: no `render json:`, serializer views used, policy_scope intact on index, FactoryBot factory present, class_name: ListingPriceHistory.name used, no hardcoded colors (useColors/successAlpha) or strings (t()) in PriceDropBadge, RTL handled via isRtl and mirrored overlay styles. | RTL ALIGNMENT BUG (PriceDropBadge.tsx, detail variant, line 82): the pill hardcodes `alignSelf: \"flex-start\"`. In ListingDetail the section column has no alignItems, so the PriceTag and title align to the right in RTL (ps/fa) while this badge clings to the LEFT edge — it visually detaches from the price it annotates. Fix: make alignSelf direction-aware, e.g. `alignSelf: isRtl ? \"flex-end\" : \"flex-start\"`. Verify visually with isRtl=true. | MINOR — DEAD textAlign on card variant (PriceDropBadge.tsx, line 60): the card '-15%' pill is a single-line pill that hugs its content, so `textAlign: isRtl ? \"right\" : \"left\"` has no visible effect. Harmless but misleading; can be dropped. Not blocking. | MINOR — TYPOGRAPHY uses raw fontSize literals (PriceDropBadge.tsx lines 57, 88: fontSize 10/11; ListingCard meta uses 11/13) rather than the documented NativeWind type scale (text-xs etc.). This matches the existing inline-style convention in PriceTag/ListingCard (the codebase deliberately uses useColors() inline styles over className for color, and sibling components already use numeric sizes), so it is consistent with the repo — flag only for awareness, not a blocker. | POSITIVE — Library/token compliance is clean: useColors() tokens only (success/successAlpha verified present in useColors.ts), no hex/rgb literals, RNR Text, lucide icon, no hand-rolled atom. Card overlay correctly flips corner via priceDropOverlayLtr/Rtl (bottom-right LTR / bottom-left RTL) and does not collide with the Seen badge (opposite corner) or StatusBadge (top). | POSITIVE — Visual hierarchy preserved: detail variant sits below the lg PriceTag and is deliberately small/subtle so it does not compete with price; ListingDetail hero gallery is GALLERY_H = SW*3/4 (well above 200px). Price remains the second hero. Trust signal (a visible price drop) is buyer-positive and reinforces deal confidence. | POSITIVE — All 3 locales present and correct: listing.priceDrop.badge and badgeCardShort exist in en/ps/fa, with ps/fa using the Arabic-Indic percent sign ٪. percent<=0 guard returns null so no empty badge renders; ListingCard/ListingDetail also guard `priceDropPercent != null && > 0` before rendering. States are handled (no badge when no drop). No loading/empty/error states are applicable to a derived badge — the parent screens own those.
- **Description**: ## Goal Buyers should know if a seller has recently lowered their price. A price-drop badge on the listing detail builds urgency and trust. Sellers benefit because serious buyers notice the drop.  ## Backend - Add ListingPriceHistory model: listing_id, old_price, new_price, currency, changed_at - After each PUT /my/listings/:id, if price changed: create a ListingPriceHistory record - Add price_drop badge/indicator on listing detail screen...
- **Acceptance**: Ship with no console errors. All mobile coding rules satisfied.
## TASK-N805
- **Title**: Seller response rate badge: show on public seller profile
- **Type**: fullstack
- **Priority**: P2
- **Status**: CHANGES_REQUESTED
- **Session**: -
- **Blocks**: -
- **BlockedBy**: -
- **ReviewNotes**: COMPLETENESS (primary): responseRatePercent is never displayed. Plumbed through user.rb, user_serializer.rb (line 24), listing_serializer.rb (line 65), users.ts (line 20), listings.ts (line 76) and asserted in users.test.ts, but grep over src/screens shows zero render sites. Both SellerProfile.tsx (lines 270-285) and ListingDetail.tsx (lines 525-539) render ONLY responseTimeLabel. The task is the 'response rate badge' — the rate % itself is invisible. Fix: render the percentage (e.g. t('...responseRate', { percent: profile.responseRatePercent }) near the label) on both screens with a new translated key in all 3 locales, OR if the time-label is the agreed badge, remove the dead responseRatePercent fields or document why they are exposed. | TRUST/CORRECTNESS (medium): A seller who NEVER replied still gets the positive-sounding badge. user.rb line 133 maps response_times.empty? → :within_a_few_days, so a 0%-rate seller shows 'Usually responds within a few days'. The mobile gates only on responseTimeLabel != null (SellerProfile.tsx:271, ListingDetail.tsx:525), so a non-responsive seller displays a reassuring badge. This is tested-as-intended (user_spec.rb:215-218) so it is a product decision, but it undermines the trust signal. Fix: consider returning nil (or a distinct 'rarely responds' label) when replied_count is 0, and gate the badge accordingly. | MAESTRO (minor): maestro/browse/seller_response_rate_badge.yaml only asserts the time-label regex 'Usually responds within.*'. It never verifies the response-rate percentage (consistent with the gap above) and relies on seed data containing a seller past the 5-conversation threshold ('In staging/CI the seed data must include such a seller') with no guarantee that exists — the flow will fail if seeds lack a qualifying seller. Fix: ensure CI seeds create a >=5-conversation seller, and add an assertion for the rate % once it is rendered. | PERFORMANCE (minor, acceptable): ListingSerializer :detailed (single listing) triggers one extra conversations+messages query per request via compute_seller_response_stats, and includes(:messages) loads all of the seller's last-90-day messages into memory. Bounded and only on detail view, and the browse :list view correctly omits the stats so the feed is not N+1'd. No change required; flagging for awareness on high-volume sellers. | VERIFIED OK: bundle exec rspec spec/models/user_spec.rb → 54 examples, 0 failures (memoization test confirms <=2 queries). npx jest users.test.ts → 8/8 pass. rubocop on user.rb/user_serializer.rb/listing_serializer.rb → 0 offenses. All 3 locales (en/ps/fa) contain within_one_hour/within_a_day/within_a_few_days. RTL handled via flexDirection isRtl on both badge rows. No hardcoded colors (useColors) or strings (t()). public_profiles_controller uses render_blue, not render json:; read-only single resource so no policy_scope needed; endpoint is auth-gated via BaseController.
- **Description**: ## Goal Buyers cannot know in advance if a seller actually responds to messages. A response-rate badge on the public seller profile (and optionally on listing detail) gives buyers a strong trust signal before they decide to message.  ## Backend - Add a response_rate computed attribute to the User model: a. Definition: percentage of conversations where the seller sent at least one message within...
- **Acceptance**: Ship with no console errors. All mobile coding rules satisfied.
