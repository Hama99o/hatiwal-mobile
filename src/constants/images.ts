/**
 * Shared image constants.
 *
 * LISTING_BLURHASH — the default blurhash placeholder shown while a remote
 * listing/photo loads. Single source of truth so every `expo-image` usage
 * references the same value instead of redeclaring the literal per file.
 */
export const LISTING_BLURHASH = "L6PZfSi_.AyE_3t7t7R**0o#DgR4";
